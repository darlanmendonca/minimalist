!function(e){var n={};function t(a){if(n[a])return n[a].exports;var i=n[a]={i:a,l:!1,exports:{}};return e[a].call(i.exports,i,i.exports,t),i.l=!0,i.exports}t.m=e,t.c=n,t.d=function(e,n,a){t.o(e,n)||Object.defineProperty(e,n,{configurable:!1,enumerable:!0,get:a})},t.r=function(e){Object.defineProperty(e,"__esModule",{value:!0})},t.n=function(e){var n=e&&e.__esModule?function(){return e.default}:function(){return e};return t.d(n,"a",n),n},t.o=function(e,n){return Object.prototype.hasOwnProperty.call(e,n)},t.p="",t(t.s="./hotsite/app.js")}({"./components/action-sheet/action-sheet.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _backdropClass = __webpack_require__(/*! ../backdrop/backdrop.class.js */ \"./components/backdrop/backdrop.class.js\");\n\nvar _backdropClass2 = _interopRequireDefault(_backdropClass);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnActionSheet extends window.HTMLElement {\n  constructor() {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    this.setStyle();\n    this._setMenu();\n    this._setCancel();\n  }\n\n  static get observedAttributes() {\n    return [];\n  }\n\n  attributeChangedCallback(name, old, value) {\n    if (this.parentNode) {\n      this[name] = value;\n    }\n  }\n\n  setStyle() {\n    this.classList.add('mn-action-sheet');\n    document.body.classList.add('mn-backdrop');\n  }\n\n  _setMenu() {\n    const menu = document.createElement('menu');\n    menu.classList.add('mn-card');\n\n    Array.from(this.querySelectorAll('option')).forEach(child => {\n      const option = document.createElement('div');\n      option.classList.add('option');\n      option.innerHTML = child.textContent;\n\n      Array.from(child.attributes).forEach(attr => option.setAttribute(attr.name, attr.value));\n\n      child.parentNode.removeChild(child);\n      menu.appendChild(option);\n    });\n\n    document.addEventListener('click', event => {\n      const isOption = event.target.classList.contains('option') && event.target.closest('.mn-action-sheet');\n      const index = Array.prototype.indexOf.call(this.menu.querySelectorAll('.option'), event.target);\n\n      if (isOption && index >= 0) {\n        const changeEvent = new Event('change');\n        changeEvent.data = { index };\n        this.dispatchEvent(changeEvent);\n      }\n    });\n\n    this.appendChild(menu);\n    this.menu = menu;\n  }\n\n  _setCancel() {\n    const button = document.createElement('button');\n\n    button.addEventListener('click', () => {\n      this.hide();\n    });\n\n    document.addEventListener('touchmove', () => {\n      const clickOutside = event.target === this;\n      this.touchmove = true;\n      if (clickOutside) {\n        event.preventDefault();\n      }\n    });\n\n    document.addEventListener('touchend', event => {\n      const clickOutside = event.target === this && !this.touchmove;\n      if (clickOutside) {\n        this.hide();\n      }\n      delete this.touchmove;\n    });\n\n    this.button = button;\n    this.appendChild(this.button);\n  }\n\n  show() {\n    this.menu.scrollTop = 0;\n    this.classList.add('visible');\n    _backdropClass2.default.show();\n    document.body.classList.add('mn-action-sheet-visible');\n  }\n\n  hide() {\n    this.classList.remove('visible');\n    _backdropClass2.default.hide();\n    document.body.classList.remove('mn-action-sheet-visible');\n  }\n}\n\nexports.default = MnActionSheet;\nwindow.customElements.define('mn-action-sheet', MnActionSheet);\n\n//# sourceURL=webpack:///./components/action-sheet/action-sheet.class.js?")},"./components/backdrop/backdrop.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\nclass MnBackdrop {\n  static show() {\n    document.body.classList.add('mn-backdrop-visible');\n  }\n\n  static hide() {\n    document.body.classList.remove('mn-backdrop-visible');\n  }\n}\n\nexports.default = MnBackdrop;\nwindow.customElements.define('mn-backdrop', MnBackdrop);\n\n//# sourceURL=webpack:///./components/backdrop/backdrop.class.js?")},"./components/button/button.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\nclass MnButton extends window.HTMLElement {\n  constructor(self) {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    this.setStyle();\n    this.setButton();\n  }\n\n  setStyle() {\n    this.classList.add('mn-button');\n  }\n\n  setButton() {\n    this.setAttribute('tabindex', '0');\n    this.addEventListener('click', () => this.blur());\n\n    document.addEventListener('keyup', event => {\n      if (event.target === this && event.key === 'Enter') {\n        this.click();\n      }\n    });\n  }\n}\n\nexports.default = MnButton;\nwindow.customElements.define('mn-button', MnButton);\n\n//# sourceURL=webpack:///./components/button/button.class.js?")},"./components/checkbox/checkbox.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _evaluateString = __webpack_require__(/*! evaluate-string */ \"./node_modules/evaluate-string/index.js\");\n\nvar _evaluateString2 = _interopRequireDefault(_evaluateString);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnCheckbox extends window.HTMLElement {\n  constructor(self) {\n    self = super(self);\n    this.ready = false;\n    return self;\n  }\n\n  connectedCallback() {\n    this.innerHTML = '';\n    this.setStyle();\n    this._setLabel();\n    this.setInput();\n    this._setCustomInput();\n    this._setForm();\n    this.checked = this.hasAttribute('checked');\n    this.disabled = this.hasAttribute('disabled');\n    this.readonly = this.hasAttribute('readonly');\n    this.name = this.hasAttribute('name');\n    this.setValidations();\n  }\n\n  static get observedAttributes() {\n    return ['value', 'name', 'disabled', 'readonly', 'autofocus'];\n  }\n\n  attributeChangedCallback(name, old, value) {\n    if (this.parentNode && this.input && this.ready) {\n      this[name] = value || this.hasAttribute(name);\n    }\n  }\n\n  setStyle() {\n    this.classList.add('mn-checkbox');\n    this.classList.add('mn-option');\n  }\n\n  _setLabel() {\n    this.label = document.createElement('label');\n    this.appendChild(this.label);\n\n    this.label.addEventListener('mouseleave', () => {\n      this.input.blur();\n    });\n\n    document.addEventListener('click', event => {\n      const isLabel = event.target.tagName === 'LABEL' && event.target.getAttribute('for') === this.id;\n\n      if (isLabel && this.id) {\n        this.input.checked = !this.input.checked;\n        this.input.dispatchEvent(new Event('change'));\n      }\n    });\n  }\n\n  setInput() {\n    this.input = document.createElement('input');\n    this.input.setAttribute('type', 'checkbox');\n    this.label.appendChild(this.input);\n\n    this.addEventListener('click', () => {\n      setTimeout(() => {\n        this.input.blur();\n      }, 0);\n    });\n\n    this.input.addEventListener('change', () => {\n      this.checked ? this.setAttribute('checked', '') : this.removeAttribute('checked');\n\n      this.form && this.form.classList && this.form.classList.contains('submitted') ? this.validate() : null;\n    });\n  }\n\n  _setCustomInput() {\n    const input = document.createElement('div');\n    input.classList.add('input');\n\n    const vector = document.createElementNS('http://www.w3.org/2000/svg', 'svg');\n    vector.innerHTML = `\n      <g\n        stroke=\"none\"\n        stroke-width=\"1\"\n        fill=\"none\"\n        fill-rule=\"evenodd\"\n        stroke-linecap=\"round\"\n        stroke-linejoin=\"round\">\n          <g\n            x=\"50%\" y=\"50%\"\n            transform=\"translate(5, 5)\"\n            stroke-width=\"2\">\n              <polyline\n                points=\"12.3825 0.581533333 3.653 10.3935333 0.273722222 6.7386\"></polyline>\n          </g>\n      </g>\n    `;\n    input.appendChild(vector);\n    this.label.appendChild(input);\n  }\n\n  _setForm() {\n    this.form = this.closest('form') || this.closest('mn-form') || document;\n  }\n\n  setValidations() {\n    this.validations = {\n      required: () => {\n        return Array.isArray(this.value) ? !this.value.some(value => value === (0, _evaluateString2.default)(this.getAttribute('value')) || value === this.getAttribute('placeholder')) : !this.value;\n      }\n    };\n  }\n\n  get checked() {\n    return this.input.checked;\n  }\n\n  set checked(value) {\n    this.input.checked = value;\n    this.input.dispatchEvent(new Event('change'));\n  }\n\n  set disabled(value) {\n    this.input.disabled = value;\n  }\n\n  set readonly(value) {\n    this.input.readOnly = value;\n  }\n\n  set name(value) {\n    const name = this.getAttribute('name');\n\n    if (name) {\n      const element = this;\n      this.input.setAttribute('name', name);\n\n      if (this.form && !this.form[name]) {\n        Object.defineProperty(this.form, name, {\n          get: () => {\n            return element.getAttribute('name') ? element : undefined;\n          }\n        });\n      }\n    }\n  }\n\n  get value() {\n    const values = this.options.filter(option => option.checked).map(option => !option.hasAttribute('value') && !option.hasAttribute('placeholder') ? this.checked : option.hasAttribute('value') ? (0, _evaluateString2.default)(option.getAttribute('value')) : option.getAttribute('placeholder'));\n\n    const isSingleOption = this.options.length === 1;\n    const isBoolean = typeof (0, _evaluateString2.default)(this.options[0].getAttribute('value')) === 'boolean' || !this.options[0].hasAttribute('value') && !this.options[0].hasAttribute('placeholder');\n\n    return isSingleOption && isBoolean ? Boolean(values[0]) : values;\n  }\n\n  set value(value) {\n    const values = Array.isArray(value) ? value : [value];\n\n    this.options.forEach(option => {\n      const check = values.some(value => value === option.getAttribute('value') || value === (0, _evaluateString2.default)(option.getAttribute('value')));\n      // console.log(value === true, check)\n      check ? option.setAttribute('checked', '') : option.removeAttribute('checked');\n      option.checked = check;\n    });\n  }\n\n  get options() {\n    const name = this.getAttribute('name') ? `[name=\"${this.getAttribute('name')}\"]` : ':not([name])';\n\n    return Array.from(this.form.querySelectorAll(`.mn-checkbox${name}`));\n  }\n\n  validate() {\n    const validations = {};\n\n    for (const attribute of Object.keys(this.validations || {})) {\n      const hasAttribute = this.hasAttribute(attribute);\n      const attributeIsInvalid = this.validations[attribute]();\n\n      if (hasAttribute && attributeIsInvalid) {\n        this.classList.add(attribute);\n        validations[attribute] = true;\n      } else {\n        this.classList.remove(attribute);\n      }\n    }\n\n    const isInvalid = Object.values(validations).some(item => item === true);\n\n    isInvalid ? this.classList.add('invalid') : this.classList.remove('invalid');\n  }\n}\n\nexports.default = MnCheckbox;\nwindow.customElements.define('mn-checkbox', MnCheckbox);\n\n//# sourceURL=webpack:///./components/checkbox/checkbox.class.js?")},"./components/code/code.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\nclass MnCode extends HTMLElement {\n  constructor(self) {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    this.setStyle();\n    this.setMarkup();\n  }\n\n  setStyle() {\n    this.classList.add('mn-code');\n  }\n\n  setMarkup() {\n    const pre = document.createElement('pre');\n    const code = document.createElement('code');\n    code.classList = this.classList;\n    code.textContent = this.textContent.replace('&lt;', '<').replace('&gt;', '>');\n\n    pre.appendChild(code);\n    this.textContent = '';\n    this.insertBefore(pre, this.firstChild);\n  }\n}\n\nexports.default = MnCode;\nwindow.customElements.define('mn-code', MnCode);\n\n//# sourceURL=webpack:///./components/code/code.class.js?")},"./components/date/date.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _inputClass = __webpack_require__(/*! ../input/input.class.js */ \"./components/input/input.class.js\");\n\nvar _inputClass2 = _interopRequireDefault(_inputClass);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnDate extends _inputClass2.default {\n  constructor(self) {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    this.innerHTML = '';\n    this.setStyle();\n    this.setInput();\n    super.setChangeEvents();\n    super.setPlaceholder();\n    super.setAttributeValue();\n    super.setAttributeDisabled();\n    super.setAttributeReadonly();\n    super.setAttributeAutofocus();\n    this.setValidations();\n  }\n\n  static get observedAttributes() {\n    return ['value', 'name', 'placeholder', 'disabled', 'readonly', 'autofocus', 'max', 'min'];\n  }\n\n  setStyle() {\n    super.setStyle();\n    this.classList.add('mn-date');\n  }\n\n  setInput() {\n    super.setInput();\n    this.input.setAttribute('type', 'date');\n    const supportsInputDate = this.input.type === 'date';\n\n    if (!supportsInputDate) {\n      this.input.setAttribute('type', 'text');\n      this.input.setAttribute('maxlength', 10);\n      this._setMask();\n    }\n  }\n\n  setValidations() {\n    super.setValidations();\n    this.validations.required = () => this.value === undefined;\n    this.validations.min = () => newDate(this.value) < newDate(this.getAttribute('min'));\n    this.validations.max = () => newDate(this.value) > newDate(this.getAttribute('max'));\n    delete this.validations.pattern;\n  }\n\n  _setMask() {\n    this.input.addEventListener('keydown', event => {\n      const isInputEditing = event.key === 'Backspace' || this.input.selectionStart !== this.input.value.length;\n\n      this.inputEditing = isInputEditing;\n    });\n\n    this.input.addEventListener('input', () => {\n      if (!this.inputEditing) {\n        this.updateMask();\n      }\n\n      this.inputEditing = undefined;\n    });\n\n    this.input.addEventListener('blur', () => {\n      this.updateMask();\n      const dateString = this.input.value.split('/').reverse().join('-');\n\n      isValidDate(dateString) ? this.updateMask() : this.value = '';\n    });\n  }\n\n  updateMask() {\n    this.input.value = this.input.value.replace(/[^\\d\\/]/, '') // disallow invalid chars\n    .replace(/[a-z]/ig, '') // disallow letters\n    .replace(/(?:^00|^(\\d{2})\\/00)/g, '$101') // disallow repeated 0\n    .replace(/000(\\d)$/g, '190$1') // disallow year 0\n    .replace(/00(\\d{2})$/g, '19$1') // disallow year 0\n    .replace(/\\/{2}/g, '/') // disallow repeated /\n    .replace(/(^\\/)/, '') // disallow / as first char\n    .replace(/(\\d+\\/\\d+\\/)\\//, '$1') // disallow third /\n    .replace(/^(\\d)\\//, '0$1/') // leading 0 day\n    .replace(/^(\\d{2})(\\d{1})/, '$1/$2') // add first /\n    .replace(/^(\\d{2}\\/)(\\d{1})\\//, '$10$2/') // leading 0 month\n    .replace(/^(\\d{2}\\/\\d{2})(\\d{1})/, '$1/$2'); // add second /\n  }\n\n  get value() {\n    let date;\n    try {\n      const isDateString = this.input.type === 'date';\n      const value = isDateString ? this.input.value : this.input.value.split('/').reverse().join('-');\n\n      date = isValidDate(value) ? newDate(value).toISOString() : undefined;\n    } catch (e) {}\n\n    return date ? date : undefined;\n  }\n\n  set value(value = '') {\n    const validDate = typeof value === 'string' && isValidDate(value);\n\n    value = value instanceof Date ? value.toISOString().substring(0, 10) : validDate ? newDate(value).toISOString().substring(0, 10) : '';\n\n    const supportsInputDate = this.input.type === 'date';\n\n    if (!supportsInputDate && validDate) {\n      const dateString = value.split('-');\n      value = new Date(dateString[0], dateString[1] - 1, dateString[2], 0, 0).toLocaleString('pt-BR').substring(0, 10);\n    }\n\n    this.input.value = value;\n    this.input.dispatchEvent(new Event('change'));\n  }\n}\n\nexports.default = MnDate;\nfunction isValidDate(dateString) {\n  const year = +dateString.split('-')[0];\n  const month = +dateString.split('-')[1];\n  const date = newDate(dateString);\n\n  return date.getFullYear() >= 1900 && date.getFullYear() === year && date.getMonth() + 1 === month;\n}\n\nfunction newDate(dateString) {\n  dateString = dateString || '';\n  const isString = typeof dateString === 'string';\n  dateString = dateString.replace(/T.+/, '');\n  dateString = isString && dateString.includes('/') ? dateString.split('/').reverse().join('-') : dateString;\n\n  dateString = dateString.split('-');\n\n  const date = new Date(dateString[0], dateString[1] - 1, dateString[2]);\n  return date;\n}\n\nwindow.customElements.define('mn-date', MnDate);\n\n//# sourceURL=webpack:///./components/date/date.class.js?")},"./components/dialog/dialog.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _backdropClass = __webpack_require__(/*! ../backdrop/backdrop.class.js */ \"./components/backdrop/backdrop.class.js\");\n\nvar _backdropClass2 = _interopRequireDefault(_backdropClass);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnDialog extends window.HTMLElement {\n  constructor(self) {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    this.setStyle();\n    this.setCard();\n    this.setButtonClose();\n    this.setOpenEvents();\n    this.setToggleEvents();\n    this.setCloseEvents();\n  }\n\n  setStyle() {\n    this.classList.add('mn-dialog');\n    document.body.classList.add('mn-backdrop');\n  }\n\n  setCard() {\n    const card = document.createElement('div');\n    card.classList.add('mn-card');\n    card.innerHTML = this.innerHTML;\n    this.innerHTML = '';\n    this.appendChild(card);\n    this.card = card;\n  }\n\n  setButtonClose() {\n    const button = document.createElement('button');\n    button.classList.add('mn-button');\n    button.classList.add('action');\n    button.setAttribute('close-dialog', '');\n    const dialog = this.querySelector('.mn-card');\n    dialog.insertBefore(button, dialog.firstChild);\n  }\n\n  setOpenEvents() {\n    document.addEventListener('click', event => {\n      if (event.target.matches(`[open-dialog=\"${this.id}\"]`)) {\n        this.open();\n        event.stopPropagation();\n      }\n    });\n  }\n\n  setToggleEvents() {\n    document.addEventListener('click', event => {\n      if (event.target.matches(`[toggle-dialog=\"${this.id}\"]`)) {\n        this.toggle();\n        event.stopPropagation();\n      }\n    });\n  }\n\n  setCloseEvents() {\n    document.addEventListener('click', event => {\n      if (event.target.matches('[close-dialog]')) {\n        this.close();\n        event.stopPropagation();\n      }\n    });\n\n    document.addEventListener('mousedown', event => {\n      const dialogVisible = this.classList.contains('visible');\n      const clickOutside = event.target.matches('.mn-dialog');\n\n      if (dialogVisible && clickOutside) {\n        this.close();\n      }\n    });\n\n    document.addEventListener('keyup', event => {\n      const esc = event.key === 'Escape';\n      const isOpened = this.classList.contains('visible');\n\n      if (esc && isOpened) {\n        this.close();\n      }\n    });\n  }\n\n  open() {\n    const previousDialog = document.querySelector('.mn-dialog.visible');\n    if (previousDialog) {\n      previousDialog.classList.remove('visible');\n    }\n\n    this.classList.add('visible');\n    this.scrollTop = 0;\n    document.body.classList.add('mn-dialog-visible');\n    _backdropClass2.default.show();\n    this.dispatchEvent(new Event('open'));\n  }\n\n  close() {\n    document.body.classList.remove('mn-dialog-visible');\n    this.classList.remove('visible');\n    _backdropClass2.default.hide();\n    this.dispatchEvent(new Event('close'));\n  }\n\n  toggle() {\n    this.classList.toggle('visible') ? this.open() : this.close();\n  }\n}\n\nexports.default = MnDialog;\nwindow.customElements.define('mn-dialog', MnDialog);\n\n//# sourceURL=webpack:///./components/dialog/dialog.class.js?")},"./components/email/email.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _inputClass = __webpack_require__(/*! ../input/input.class.js */ \"./components/input/input.class.js\");\n\nvar _inputClass2 = _interopRequireDefault(_inputClass);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnEmail extends _inputClass2.default {\n  constructor(self) {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    super.connectedCallback();\n    this.classList.add('mn-email');\n    this.input.setAttribute('type', 'email');\n    const regex = '^[a-zA-Z0-9.!#$%&’*+/=?^_`{|}~-]+@[a-zA-Z0-9-]+(?:\\.[a-zA-Z0-9-]+)*';\n    this.setAttribute('pattern', this.getAttribute('pattern') || regex);\n  }\n}\n\nexports.default = MnEmail;\nwindow.customElements.define('mn-email', MnEmail);\n\n//# sourceURL=webpack:///./components/email/email.class.js?")},"./components/form/form.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\nclass MnForm extends window.HTMLElement {\n  constructor(self) {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    this.setStyle();\n    this.setSubmit();\n    this.setReset();\n    this.setEmpty();\n    this.setAttributeDisabled();\n    this.setAttributeReadonly();\n  }\n\n  static get observedAttributes() {\n    return ['name', 'disabled', 'readonly'];\n  }\n\n  attributeChangedCallback(name, old, value) {\n    this[name] = value;\n  }\n\n  setStyle() {\n    this.classList.add('mn-form');\n  }\n\n  setSubmit() {\n    document.addEventListener('keydown', event => {\n      const enter = event.key === 'Enter';\n      const srcElementInsideForm = event.target.closest('mn-form');\n      if (enter && srcElementInsideForm) {\n        this.submit();\n      }\n    });\n\n    document.addEventListener('click', event => {\n      const isButtonSubmit = (event.target.matches('button[type=\"submit\"]') || event.target.matches('mn-button[submit]')) && event.target.closest('mn-form') === this;\n\n      if (isButtonSubmit) {\n        this.submit();\n      }\n    });\n  }\n\n  setReset() {\n    document.addEventListener('click', event => {\n      const isButtonSubmit = (event.target.matches('button[type=\"reset\"]') || event.target.matches('mn-button[reset]')) && event.target.closest('mn-form') === this;\n\n      if (isButtonSubmit) {\n        this.reset();\n      }\n    });\n  }\n\n  setEmpty() {\n    document.addEventListener('click', event => {\n      const isButtonSubmit = (event.target.matches('button[type=\"empty\"]') || event.target.matches('mn-button[empty]')) && event.target.closest('mn-form') === this;\n\n      if (isButtonSubmit) {\n        this.empty();\n      }\n    });\n  }\n\n  setAttributeDisabled() {\n    this.disabled = this.hasAttribute('disabled');\n  }\n\n  setAttributeReadonly() {\n    this.readonly = this.hasAttribute('readonly');\n  }\n\n  validate() {\n    this.dispatchEvent(new Event('validate'));\n    this.inputs.filter(input => !input.hasAttribute('disabled') && !input.hasAttribute('readonly')).forEach(input => input.validate());\n\n    const isInvalid = !this.querySelector('.invalid');\n    return isInvalid;\n  }\n\n  reset() {\n    this.classList.remove('submitted');\n    Object.keys(this.data).forEach(name => {\n      if (this[name]) {\n        this[name].value = this.defaults[name];\n\n        const validations = Object.keys(this[name].validations);\n        validations.push('invalid');\n        validations.forEach(validationClass => this[name].classList.remove(validationClass));\n      }\n    });\n  }\n\n  empty() {\n    this.classList.remove('submitted');\n    Object.keys(this.data).forEach(name => {\n      this[name].value = undefined;\n    });\n  }\n\n  get inputs() {\n    return Array.from(this.querySelectorAll('.mn-input, .mn-checkbox, .mn-radio'));\n  }\n\n  get defaults() {\n    const defaults = {};\n\n    this.inputs.forEach(input => {\n      const name = input.getAttribute('name');\n\n      if (name) {\n        defaults[name] = input.default;\n      }\n    });\n\n    return defaults;\n  }\n\n  get data() {\n    const data = {};\n\n    this.inputs.forEach(input => {\n      const name = input.getAttribute('name');\n\n      if (name) {\n        data[name] = input.value;\n      }\n    });\n\n    return data;\n  }\n\n  set name(name) {\n    if (name && typeof name === 'string') {\n      window[name] = this;\n    }\n  }\n\n  set disabled(value) {\n    this.inputs.forEach(input => {\n      this.hasAttribute('disabled') ? input.setAttribute('disabled', 'true') : input.removeAttribute('disabled');\n    });\n  }\n\n  set readonly(value) {\n    this.inputs.forEach(input => {\n      this.hasAttribute('readonly') ? input.setAttribute('readonly', 'true') : input.removeAttribute('readonly');\n    });\n  }\n\n  submit() {\n    this.classList.add('submitted');\n    const isValid = this.validate();\n    const event = new Event('submit');\n    event.data = this.data;\n\n    if (isValid) {\n      this.dispatchEvent(event);\n    }\n  }\n}\n\nexports.default = MnForm;\nwindow.customElements.define('mn-form', MnForm);\n\n//# sourceURL=webpack:///./components/form/form.class.js?")},"./components/hidden/hidden.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _inputClass = __webpack_require__(/*! ../input/input.class.js */ \"./components/input/input.class.js\");\n\nvar _inputClass2 = _interopRequireDefault(_inputClass);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnHidden extends _inputClass2.default {\n  constructor(self) {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    this.innerHTML = '';\n    this.setStyle();\n    this.setInput();\n    super.setChangeEvents();\n    super.setAttributeValue();\n    super.setAttributeName();\n    super.setAttributeDisabled();\n  }\n\n  static get observedAttributes() {\n    return ['value', 'name', 'disabled'];\n  }\n\n  setStyle() {\n    super.setStyle();\n    this.classList.add('mn-hidden');\n  }\n\n  setInput() {\n    super.setInput();\n    this.input.setAttribute('type', 'hidden');\n  }\n}\n\nexports.default = MnHidden;\nwindow.customElements.define('mn-hidden', MnHidden);\n\n//# sourceURL=webpack:///./components/hidden/hidden.class.js?")},"./components/image/image.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\nclass MnImage extends window.HTMLElement {\n  constructor(self) {\n    self = super(self);\n    this.setStyle();\n    this.setImage();\n    this.setOutline();\n    this.setRotation();\n    this.setBlur();\n    this.setClick();\n    return self;\n  }\n\n  setStyle() {\n    this.classList.add('mn-image');\n  }\n\n  setOutline() {\n    if (this.parentElement.tagName === 'A') {\n      this.parentElement.classList.add('no-outline');\n    }\n  }\n\n  setImage() {\n    let element = this;\n    let attributes = [{\n      name: 'src'\n    }, {\n      name: 'alt'\n    }, {\n      name: 'title'\n    }, {\n      name: 'draggable'\n    }];\n\n    // image element\n    let image = document.createElement('img');\n    attributes.map(setAttribute);\n    this.appendChild(image);\n\n    function setAttribute(attribute) {\n      let isDefaultAttribute = attribute.hasOwnProperty('default');\n      let attributeValue = element.getAttribute(attribute.name);\n\n      if (isDefaultAttribute) {\n        let isValidValue = attribute.hasOwnProperty('values') && attribute.values.indexOf(attributeValue) >= 0;\n\n        let value = isValidValue ? attributeValue : attribute.default;\n\n        image.setAttribute(attribute.name, value);\n      } else if (attributeValue) {\n        image.setAttribute(attribute.name, attributeValue);\n      }\n    }\n  }\n\n  setRotation() {\n    this.addEventListener('mouseenter', setRotation3d);\n    this.addEventListener('mousemove', setRotation3d);\n    this.addEventListener('mouseleave', unsetRotate3d);\n\n    function setRotation3d(event) {\n      if (this.parentElement.tagName === 'A') {\n        let bounds = this.getBoundingClientRect();\n        let isTouchEvent = event.type.startsWith('touch');\n\n        let clientX = isTouchEvent ? event.touches[0].clientX : event.clientX;\n\n        let clientY = isTouchEvent ? event.touches[0].clientY : event.clientY;\n\n        let percentX = (clientX - bounds.left) / bounds.width;\n        let percentY = (clientY - bounds.top) / bounds.height;\n        let angles = 20;\n        let rotateY = angles * (-percentX * 2) + angles;\n        let rotateX = angles * (percentY * 2) - angles;\n        this.style.transform = `\n          scale(1.07)\n          perspective(1000px)\n          rotateY(${rotateY}deg)\n          rotateX(${rotateX}deg)\n        `;\n      }\n    }\n\n    function unsetRotate3d() {\n      this.style.transform = '';\n    }\n  }\n\n  setBlur() {\n    if (this.parentElement.tagName === 'A') {\n      this.parentElement.addEventListener('click', blur);\n      this.parentElement.addEventListener('mouseleave', blur);\n      this.parentElement.addEventListener('touchmove', blur);\n    }\n\n    function blur() {\n      this.blur();\n    }\n  }\n\n  setClick() {\n    if (this.parentElement.tagName === 'A') {\n      this.parentElement.addEventListener('mousedown', clicked);\n      this.parentElement.addEventListener('mouseup', unclicked);\n    }\n\n    let oldTransform;\n\n    function clicked() {\n      let mnImage = this.querySelector('mn-image');\n      oldTransform = mnImage.style.transform;\n      let transform = mnImage.style.transform.replace('scale(1.07)', 'scale(1.03)').replace(/rotateX(.+)/, 'rotateX(0)').replace(/rotateY(.+)/, 'rotateY(0)');\n\n      mnImage.style.transform = transform;\n    }\n\n    function unclicked() {\n      let mnImage = this.querySelector('mn-image');\n      mnImage.style.transform = oldTransform;\n    }\n  }\n}\nexports.default = MnImage;\n\n//# sourceURL=webpack:///./components/image/image.class.js?")},"./components/input/input.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _evaluateString = __webpack_require__(/*! evaluate-string */ \"./node_modules/evaluate-string/index.js\");\n\nvar _evaluateString2 = _interopRequireDefault(_evaluateString);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnInput extends window.HTMLElement {\n  constructor(self) {\n    self = super(self);\n    this.delimeterKeys = ['Comma', 'Enter', 'Space'];\n    return self;\n  }\n\n  connectedCallback() {\n    this.innerHTML = '';\n    this.trimValue = true;\n    this.setStyle();\n    this.setValidations();\n    this.setInput();\n    this.setChangeEvents();\n    this.setPlaceholder();\n    this.setAttributeValue();\n    this.setAttributeName();\n    this.setAttributeDisabled();\n    this.setAttributeReadonly();\n    this.setAttributeMaxlength();\n    this.setAttributeAutocapitalize();\n    this.setAttributeAutocomplete();\n    this.setAttributeSpellcheck();\n    this.setAttributeAutofocus();\n  }\n\n  static get observedAttributes() {\n    return ['value', 'name', 'placeholder', 'disabled',\n    // 'readonly',\n    'maxlength', 'autocapitalize', 'autofocus'];\n  }\n\n  attributeChangedCallback(name, old, value) {\n    this[name] = value;\n  }\n\n  setStyle() {\n    this.classList.add('mn-input');\n  }\n\n  setInput() {\n    this.input = document.createElement('input');\n    this.input.classList.add('input');\n\n    this.appendChild(this.input);\n\n    this.input.addEventListener('input', () => this.setValue);\n\n    this.input.addEventListener('change', () => {\n      // set class .has-value\n      if (this.trimValue && document.activeElement !== this.input) {\n        this.input.value = this.input.value.replace(/\\s{2,}/g, ' ').trim();\n      }\n\n      this.dispatchChangeEvent();\n      this.setValue();\n    });\n\n    this.input.addEventListener('keydown', event => {\n      const isDelimeterKey = this.delimeterKeys.find(key => key === event.code);\n      if (this.hasAttribute('multiple') && isDelimeterKey) {\n        this.input.dispatchEvent(new Event('blur'));\n        event.preventDefault();\n      }\n    });\n\n    this.input.addEventListener('blur', () => {\n      if (this.input.value && this.hasAttribute('multiple')) {\n        if (this.delimeterKeys.length) {\n          this.push(this.input.value);\n        }\n        this.input.value = '';\n      }\n\n      this.setValue();\n    });\n\n    const validate = () => {\n      // validate\n      const closestForm = this.closest('form') || this.closest('mn-form');\n      closestForm && closestForm.classList.contains('submitted') ? this.validate() : null;\n    };\n\n    this.input.addEventListener('keyup', validate);\n    this.input.addEventListener('change', validate);\n    this.input.addEventListener('input', validate);\n\n    this.input.addEventListener('focus', () => {\n      if (!this.hasAttribute('readonly') && !this.hasAttribute('disabled')) {\n        this.classList.add('focus');\n      }\n    });\n\n    this.input.addEventListener('blur', () => this.classList.remove('focus'));\n    if (this.hasAttribute('value')) {\n      this.input.value = this.getAttribute('value');\n    }\n    this.setValue();\n  }\n\n  setValue() {\n    this.input.value || this.hasAttribute('value') ? this.classList.add('has-value') : this.classList.remove('has-value');\n  }\n\n  setChangeEvents() {\n    this.input.addEventListener('input', this.dispatchChangeEvent);\n    this.input.addEventListener('blur', this.dispatchChangeEvent);\n  }\n\n  dispatchChangeEvent() {\n    this.dispatchEvent(new Event('change'));\n  }\n\n  setPlaceholder() {\n    this.label = document.createElement('label');\n    this.label.classList.add('placeholder');\n    this.appendChild(this.label);\n    this.placeholder = this.getAttribute('placeholder');\n  }\n\n  setAttributeValue() {\n    this.value = (0, _evaluateString2.default)(this.getAttribute('value') || '');\n    this.default = this.value;\n  }\n\n  setAttributeName() {\n    const name = this.getAttribute('name');\n    if (name) {\n      this.name = name;\n    }\n  }\n\n  setAttributeDisabled() {\n    this.disabled = this.hasAttribute('disabled');\n  }\n\n  setAttributeReadonly() {\n    this.readonly = this.hasAttribute('readonly');\n  }\n\n  setAttributeMaxlength() {\n    this.maxlength = this.getAttribute('maxlength');\n  }\n\n  setAttributeAutocapitalize() {\n    this.autocapitalize = this.getAttribute('autocapitalize') || 'off';\n  }\n\n  setAttributeAutocomplete() {\n    this.input.setAttribute('autocomplete', 'off');\n  }\n\n  setAttributeSpellcheck() {\n    this.input.setAttribute('spellcheck', 'off');\n  }\n\n  setAttributeAutofocus() {\n    this.autofocus = this.hasAttribute('autofocus');\n  }\n\n  setValidations() {\n    this.validations = {\n      required: () => this.hasAttribute('multiple') ? this.value.length === 0 : this.value === undefined,\n      pattern: () => {\n        const reg = new RegExp(this.getAttribute('pattern'));\n\n        return this.value ? !reg.test(this.value) : false;\n      }\n    };\n  }\n\n  get value() {\n    return this.hasAttribute('multiple') ? (0, _evaluateString2.default)(this.getAttribute('value')) ? (0, _evaluateString2.default)(this.getAttribute('value')).map(item => String(item)) : [] : this.input.value || undefined;\n  }\n\n  set value(value = '') {\n    if (this.input) {\n      const differentValue = this.getAttribute('value') !== value;\n      const valueIsMultiple = this.hasAttribute('multiple');\n\n      if (!valueIsMultiple) {\n        this.input.value = value;\n      }\n\n      if (differentValue) {\n        if (valueIsMultiple) {\n          Array.from(this.querySelectorAll('.value')).forEach(item => item.parentNode.removeChild(item));\n\n          const values = Array.isArray(value) ? value.map(item => String(item)) : [value];\n\n          values.filter(item => item).forEach(val => this.push(val));\n          this.input.value = '';\n        } else {\n          this.input.value = value;\n\n          if (document.activeElement !== this.input) {\n            this.input.value = this.trimValue && value ? value.replace(/\\s{2,}/g, ' ').trim() : value;\n          }\n        }\n\n        this.input.dispatchEvent(new Event('change'));\n      }\n    }\n  }\n\n  set name(value) {\n    const form = this.closest('form') || this.closest('mn-form');\n    const name = this.getAttribute('name');\n    const element = this;\n\n    if (form && !form[name]) {\n      Object.defineProperty(form, name, {\n        get: () => {\n          return element.getAttribute('name') ? element : undefined;\n        }\n      });\n    }\n  }\n\n  set placeholder(value) {\n    if (this.label) {\n      this.label.textContent = value;\n    }\n  }\n\n  set disabled(value) {\n    if (this.input) {\n      this.input.disabled = value || this.hasAttribute('disabled');\n    }\n  }\n\n  set readonly(value) {\n    if (this.input) {\n      this.input.readOnly = value || this.hasAttribute('readonly');\n    }\n  }\n\n  set maxlength(value) {\n    if (this.input) {\n      value ? this.input.setAttribute('maxlength', value) : this.input.removeAttribute('maxlength');\n    }\n  }\n\n  set autocapitalize(value) {\n    if (this.input) {\n      value ? this.input.setAttribute('autocapitalize', value) : this.input.removeAttribute('autocapitalize');\n    }\n  }\n\n  set autofocus(value) {\n    if (this.input) {\n      this.input.autofocus = value || this.hasAttribute('autofocus');\n    }\n  }\n\n  blur() {\n    this.input.blur();\n  }\n\n  focus() {\n    // this.input.dispatchEvent(new Event('touchstart'))\n    // this.input.dispatchEvent(new Event('touchsend'))\n    this.input.focus();\n  }\n\n  validate() {\n    const validations = {};\n\n    for (const attribute of Object.keys(this.validations || {})) {\n      const hasAttribute = this.hasAttribute(attribute);\n      const attributeIsInvalid = this.validations[attribute]();\n\n      if (hasAttribute && attributeIsInvalid) {\n        this.classList.add(attribute);\n        validations[attribute] = true;\n      } else {\n        this.classList.remove(attribute);\n      }\n    }\n\n    const isInvalid = Object.values(validations).some(item => item === true);\n\n    isInvalid ? this.classList.add('invalid') : this.classList.remove('invalid');\n  }\n\n  push(value, text) {\n    const values = Array.from(this.querySelectorAll('.value')).map(item => item.hasAttribute('value') ? item.getAttribute('value') : item.textContent);\n\n    const attributeValue = this.getAttribute('value');\n\n    const itemUsed = values.find(item => item === value);\n    this.classList.add('has-value');\n\n    if (!itemUsed) {\n      const item = document.createElement('div');\n      const buttonClose = document.createElement('button');\n      buttonClose.addEventListener('click', event => this.remove(event.target.parentNode));\n      item.classList.add('value');\n      item.textContent = text || value[this.keyValue] || value;\n      item.appendChild(buttonClose);\n      value = typeof value === 'string' ? (0, _evaluateString2.default)(value) : value;\n\n      item.setAttribute('value', typeof value === 'object' ? JSON.stringify(value) : value);\n      this.insertBefore(item, this.input);\n\n      const values = Array.from(this.querySelectorAll('.value')).map(item => (0, _evaluateString2.default)(item.getAttribute('value')) || item.textContent);\n\n      if (this.hasAttribute('multiple')) {\n        this.setAttribute('value', JSON.stringify(values));\n      }\n    }\n\n    const changeAttributeValue = attributeValue !== this.getAttribute('value');\n\n    if (changeAttributeValue) {\n      this.dispatchEvent(new Event('change'));\n    }\n  }\n\n  remove(item) {\n    item.parentNode.removeChild(item);\n\n    const values = Array.from(this.querySelectorAll('.value')).map(item => {\n      return item.hasAttribute('value') ? (0, _evaluateString2.default)(item.getAttribute('value')) : item.textContent;\n    });\n\n    values.length ? this.setAttribute('value', JSON.stringify(values)) : this.removeAttribute('value');\n\n    this.input.dispatchEvent(new Event('change'));\n    this.dispatchEvent(new Event('change'));\n    this.input.value = '';\n  }\n}\n\nexports.default = MnInput;\nwindow.customElements.define('mn-input', MnInput);\n\n//# sourceURL=webpack:///./components/input/input.class.js?")},"./components/input/input.react.jsx":function(module,exports,__webpack_require__){"use strict";eval('\n\nObject.defineProperty(exports, "__esModule", {\n  value: true\n});\n\nvar _react = __webpack_require__(/*! react */ "./node_modules/react/index.js");\n\nvar _react2 = _interopRequireDefault(_react);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnInput extends _react.Component {\n  render() {\n    return _react2.default.createElement(\'mn-input\', this.props);\n  }\n}\nexports.default = MnInput;\n\n//# sourceURL=webpack:///./components/input/input.react.jsx?')},"./components/list/list.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _dragula = __webpack_require__(/*! dragula */ \"./node_modules/dragula/dragula.js\");\n\nvar _dragula2 = _interopRequireDefault(_dragula);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnList extends window.HTMLElement {\n  constructor(self) {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    this.setStyle();\n    this.setCollapse();\n    this.setDraggable();\n  }\n\n  setStyle() {\n    this.classList.add('mn-list');\n  }\n\n  setCollapse() {\n    document.addEventListener('click', event => {\n      const item = event.target.closest('.mn-item[collapse]');\n      const isItemCollapse = event.target.matches('.mn-item[collapse]') || item && event.target.tagName !== 'A' && event.target.tagName !== 'BUTTON' && event.target.tagName !== 'MN-BUTTON';\n      const isListOwnerOfItem = event.target.closest('.mn-list') === this;\n\n      if (isItemCollapse && isListOwnerOfItem) {\n        const detailVisible = item.classList.contains('detail-visible');\n        if (detailVisible) {\n          item.classList.remove('detail-visible');\n          Array.from(item.querySelectorAll('.detail-visible')).forEach(item => item.classList.remove('detail-visible'));\n        } else {\n          item.classList.add('detail-visible');\n        }\n        event.stopPropagation();\n      }\n    });\n  }\n\n  setDraggable() {\n    if (!MnList.draggableSettings) {\n      let originIndex;\n      const options = {\n        moves(element) {\n          originIndex = Array.prototype.indexOf.call(element.closest('.mn-list').querySelectorAll('.mn-item'), element);\n          return element.matches('.mn-item[draggable]');\n        },\n        accepts(element, target) {\n          const parent = element.closest('.mn-list');\n          const parentName = parent.getAttribute('name');\n          const targetName = target.getAttribute('name');\n\n          const move = parent === target || parent.hasAttribute('name') && target.hasAttribute('name') && parentName === targetName;\n\n          return move;\n        },\n        direction: 'vertical',\n        mirrorContainer: document.body\n      };\n\n      MnList.draggableSettings = (0, _dragula2.default)(options);\n\n      MnList.draggableSettings.on('drop', (element, target, source) => {\n        const targetIndex = Array.prototype.indexOf.call(target.querySelectorAll('.mn-item'), element);\n\n        const reorder = source === target;\n        const rearrange = source !== target;\n\n        if (reorder) {\n          // reorder inside same list\n          if (originIndex !== targetIndex) {\n            const event = new Event('reorder');\n            event.originIndex = originIndex;\n            event.targetIndex = targetIndex;\n            source.dispatchEvent(event);\n          }\n        } else if (rearrange) {\n          // rearrange to another list\n          const event = new Event('rearrange');\n          event.origin = source;\n          event.element = element;\n          event.targetList = target;\n          event.originIndex = originIndex;\n          event.targetIndex = targetIndex;\n          source.dispatchEvent(event);\n        }\n      });\n    }\n\n    MnList.draggableSettings.containers.push(this);\n  }\n}\n\nexports.default = MnList;\nwindow.customElements.define('mn-list', MnList);\n\n//# sourceURL=webpack:///./components/list/list.class.js?")},"./components/number/number.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _inputClass = __webpack_require__(/*! ../input/input.class.js */ \"./components/input/input.class.js\");\n\nvar _inputClass2 = _interopRequireDefault(_inputClass);\n\nvar _evaluateString = __webpack_require__(/*! evaluate-string */ \"./node_modules/evaluate-string/index.js\");\n\nvar _evaluateString2 = _interopRequireDefault(_evaluateString);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnNumber extends _inputClass2.default {\n  constructor(self) {\n    self = super(self);\n    this.delimeterKeys = ['Enter', 'Space'];\n    return self;\n  }\n\n  connectedCallback() {\n    this.innerHTML = '';\n    this.setStyle();\n    super.setInput();\n    this.setChangeEvents();\n    this._setMask();\n    this._setMobileKeyboard();\n    this.setInputTransforms();\n    this.setInputKeys();\n    super.setPlaceholder();\n    super.setAttributeValue();\n    super.setAttributeName();\n    super.setAttributeDisabled();\n    super.setAttributeReadonly();\n    super.setAttributeAutofocus();\n    this._setAttributeMax();\n    this._setAttributeMin();\n    this.setValidations();\n  }\n\n  static get observedAttributes() {\n    return ['value', 'name', 'placeholder', 'disabled', 'readonly', 'autofocus', 'max', 'min'];\n  }\n\n  setStyle() {\n    super.setStyle();\n    this.classList.add('mn-number');\n  }\n\n  _setMask() {\n    this.mask = document.createElement('div');\n    this.mask.classList.add('mask');\n    this.appendChild(this.mask);\n\n    this.input.addEventListener('input', () => {\n      this.updateMask();\n    });\n  }\n\n  _setMobileKeyboard() {\n    this.input.setAttribute('pattern', '\\\\d*');\n  }\n\n  setInputTransforms() {\n    this.input.addEventListener('change', () => {\n      const commaOrDot = !this.input.value.endsWith(',') && !this.input.value.endsWith('.');\n\n      if (commaOrDot) {\n        try {\n          const value = eval(this.input.value.replace(/,/g, '.'));\n\n          value !== undefined ? this.input.value = String(value).replace(/\\./g, ',') : null;\n          const valueIsDefined = value !== undefined;\n\n          if (valueIsDefined) {\n            const isCurrency = this.hasAttribute('currency');\n            const precision = this.getAttribute('precision') || 0;\n\n            switch (true) {\n              case isCurrency:\n                this.input.value = value.toFixed(precision || 2).replace(/\\./g, ',');\n                break;\n\n              default:\n                this.input.value = precision ? value.toFixed(precision).replace(/\\./g, ',') : String(value).replace(/\\./g, ',');\n                break;\n            }\n          }\n        } catch (e) {\n          this.value = undefined;\n          this.input.value = '';\n        }\n\n        this.hasAttribute('percentage') ? this.updateMask() : null;\n\n        if (this.input.value === '') {\n          this.removeAttribute('value');\n        }\n      }\n    });\n  }\n\n  setChangeEvents() {\n    this.input.addEventListener('blur', this.dispatchChangeEvent);\n  }\n\n  setInputKeys() {\n    this.input.addEventListener('keydown', event => {\n      if (!this.hasAttribute('readonly')) {\n        const step = this.hasAttribute('percentage') ? +this.getAttribute('step') * 100 / 10000 || 0.01 : +this.getAttribute('step') || 1;\n        const value = this.value || 0;\n\n        switch (event.key) {\n          case 'ArrowUp':\n            this.value = value + step;\n            break;\n          case 'ArrowDown':\n            this.value = value - step;\n            break;\n        }\n\n        event.key === 'ArrowUp' || event.key === 'ArrowDown' ? event.preventDefault() : null;\n      }\n    });\n  }\n\n  _setAttributeMax() {\n    this.max = this.getAttribute('max');\n  }\n\n  _setAttributeMin() {\n    this.min = this.getAttribute('min');\n  }\n\n  setValidations() {\n    super.setValidations();\n    this.validations.required = () => this.hasAttribute('multiple') ? this.value.length === 0 : this.value === undefined;\n    this.validations.min = () => this.value < this.getAttribute('min');\n    this.validations.max = () => this.value > this.getAttribute('max');\n    delete this.validations.pattern;\n  }\n\n  get value() {\n    const isUndefined = this.input.value === '' && !this.hasAttribute('value');\n    const value = this.hasAttribute('value') ? this.getAttribute('value') : this.input.value;\n    const numberString = value.replace(/,/g, '.');\n\n    const val = isUndefined ? undefined : this.hasAttribute('percentage') ? numberString * 100 / 10000 : parseFloat(numberString);\n\n    if (isUndefined) {\n      return undefined;\n    } else {\n      if (this.hasAttribute('multiple')) {\n        return (0, _evaluateString2.default)(this.getAttribute('value')) ? (0, _evaluateString2.default)(this.getAttribute('value')).map(item => +String(item).replace(',', '.')) : [];\n      } else {\n        return this.hasAttribute('percentage') ? numberString * 100 / 10000 : parseFloat(numberString);\n      }\n    }\n\n    return val;\n  }\n\n  set value(value) {\n    const valueIsMultiple = this.hasAttribute('multiple');\n    const differentValue = this.getAttribute('value') !== value;\n    const hasValue = value !== '';\n\n    if (this.input && hasValue && differentValue) {\n      if (valueIsMultiple && value && Array.isArray(value)) {\n        Array.from(this.querySelectorAll('.value')).forEach(item => item.parentNode.removeChild(item));\n\n        const values = Array.isArray(value) ? value.map(item => String(item)) : [value].filter(item => item !== undefined);\n\n        if (!values.length) {\n          this.removeAttribute('value');\n        }\n\n        values.filter(item => item).forEach(val => {\n          // val = eval(String(val).replace(/,/g, '.'))\n\n          // if (val !== undefined && differentValue) {\n          //   val = this.hasAttribute('percentage')\n          //     ? +(val * 100).toFixed(this.getAttribute('precision') || 2)\n          //     : val\n          // }\n          this.push(val);\n        });\n      } else {\n        try {\n          value = eval(String(value).replace(/,/g, '.'));\n\n          if (value !== undefined && differentValue) {\n            value = this.hasAttribute('percentage') ? +(value * 100).toFixed(this.getAttribute('precision') || 2) : value;\n            hasValue ? this.setAttribute('value', value) : this.removeAttribute('value');\n            this.input.value = value;\n          } else {\n            this.input.value = '';\n          }\n        } catch (e) {\n          this.input.value = '';\n        }\n      }\n\n      this.input.dispatchEvent(new Event('change'));\n      this.input.dispatchEvent(new Event('input'));\n    }\n  }\n\n  set max(value) {\n    if (this.label) {\n      this.hasAttribute('max') ? this.label.setAttribute('max', value) : this.label.removeAttribute('max');\n    }\n  }\n\n  set min(value) {\n    if (this.label) {\n      this.hasAttribute('min') ? this.label.setAttribute('min', value) : this.label.removeAttribute('min');\n    }\n  }\n\n  updateMask() {\n    const hasValue = this.input.value !== '' && !/^\\s+$/.test(this.input.value);\n\n    if (this.mask && this.hasAttribute('percentage')) {\n      const text = hasValue ? `${this.input.value} %` : '';\n\n      this.mask.textContent = text;\n    }\n  }\n}\n\nexports.default = MnNumber;\nwindow.customElements.define('mn-number', MnNumber);\n\n//# sourceURL=webpack:///./components/number/number.class.js?")},"./components/password/password.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _inputClass = __webpack_require__(/*! ../input/input.class.js */ \"./components/input/input.class.js\");\n\nvar _inputClass2 = _interopRequireDefault(_inputClass);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnPassword extends _inputClass2.default {\n  constructor(self) {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    this.innerHTML = '';\n    this.setStyle();\n    this.setInput();\n    super.setChangeEvents();\n    super.setPlaceholder();\n    this._setVisibilityButton();\n    super.setAttributeValue();\n    super.setAttributeName();\n    super.setAttributeDisabled();\n    super.setAttributeReadonly();\n    super.setAttributeAutofocus();\n    super.setValidations();\n  }\n\n  static get observedAttributes() {\n    return ['value', 'name', 'placeholder', 'disabled', 'readonly', 'autofocus'];\n  }\n\n  setStyle() {\n    super.setStyle();\n    this.classList.add('mn-password');\n  }\n\n  setInput() {\n    super.setInput();\n    this.input.setAttribute('type', 'password');\n  }\n\n  _setVisibilityButton() {\n    const button = document.createElement('button');\n    button.setAttribute('type', 'button');\n    button.setAttribute('tabindex', '-1');\n\n    this.appendChild(button);\n    this.button = button;\n    this.input.addEventListener('blur', () => {\n      this.input.setAttribute('type', 'password');\n      this.classList.remove('show-password');\n      this.input.dispatchEvent(new Event('change'));\n    });\n\n    button.addEventListener('mousedown', event => {\n      event.preventDefault();\n    });\n\n    button.addEventListener('click', () => {\n      const toggledType = this.input.getAttribute('type') === 'password' ? 'text' : 'password';\n      this.input.setAttribute('type', toggledType);\n      this.classList.toggle('show-password');\n      this.input.focus();\n    });\n  }\n}\n\nexports.default = MnPassword;\nwindow.customElements.define('mn-password', MnPassword);\n\n//# sourceURL=webpack:///./components/password/password.class.js?")},"./components/radio/radio.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _checkboxClass = __webpack_require__(/*! ../checkbox/checkbox.class.js */ \"./components/checkbox/checkbox.class.js\");\n\nvar _checkboxClass2 = _interopRequireDefault(_checkboxClass);\n\nvar _evaluateString = __webpack_require__(/*! evaluate-string */ \"./node_modules/evaluate-string/index.js\");\n\nvar _evaluateString2 = _interopRequireDefault(_evaluateString);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnRadio extends _checkboxClass2.default {\n  constructor(self) {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    this.innerHTML = '';\n    this.setStyle();\n    super._setLabel();\n    this.setInput();\n    this._setCustomInput();\n    this._setForm();\n    // this.checked = this.hasAttribute('checked')\n    this.disabled = this.hasAttribute('disabled');\n    this.readonly = this.hasAttribute('readonly');\n    this.name = this.hasAttribute('name');\n    this.setValidations();\n  }\n\n  setStyle() {\n    this.classList.add('mn-radio');\n    this.classList.add('mn-option');\n  }\n\n  setInput() {\n    this.input = document.createElement('input');\n    this.input.setAttribute('type', 'radio');\n    this.label.appendChild(this.input);\n\n    this.input.addEventListener('change', event => {\n      this.checked ? this.setAttribute('checked', '') : this.removeAttribute('checked');\n\n      this.options.forEach(option => {\n        if (option !== event.target.closest('mn-radio')) {\n          option.removeAttribute('checked');\n          option.input.checked = false;\n        }\n\n        option.form && option.form.classList && option.form.classList.contains('submitted') ? option.validate() : null;\n      });\n    });\n  }\n\n  _setCustomInput() {\n    const input = document.createElement('div');\n    input.classList.add('input');\n\n    this.label.appendChild(input);\n  }\n\n  setValidations() {\n    this.validations = {\n      required: () => !this.value\n    };\n  }\n\n  get options() {\n    const name = this.getAttribute('name') ? `[name=\"${this.getAttribute('name')}\"]` : ':not([name])';\n\n    return Array.from(this.form.querySelectorAll(`.mn-radio${name}`));\n  }\n\n  get value() {\n    const value = this.options.filter(option => option.checked).map(option => option.hasAttribute('value') ? (0, _evaluateString2.default)(option.getAttribute('value')) : option.getAttribute('placeholder'));\n\n    return value[0];\n  }\n\n  set value(value) {\n    this.options.forEach(option => {\n      option.checked = false;\n    });\n\n    const option = this.options.find(option => (0, _evaluateString2.default)(option.getAttribute('value')) === value);\n\n    if (option) {\n      option.checked = true;\n    }\n  }\n}\n\nexports.default = MnRadio;\nwindow.customElements.define('mn-radio', MnRadio);\n\n//# sourceURL=webpack:///./components/radio/radio.class.js?")},"./components/search/search.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _selectClass = __webpack_require__(/*! ../select/select.class.js */ \"./components/select/select.class.js\");\n\nvar _selectClass2 = _interopRequireDefault(_selectClass);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnSearch extends _selectClass2.default {\n  constructor(self) {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    super.connectedCallback();\n    this.setLoading();\n    this.setSearchSheet();\n  }\n\n  setStyle() {\n    super.setStyle();\n    this.classList.add('mn-search');\n  }\n\n  setLoading() {\n    const loading = document.createElement('div');\n    loading.classList.add('loading');\n    this.appendChild(loading);\n  }\n\n  setSearchSheet() {\n    if (this.actionSheet) {\n      this.actionSheet = undefined;\n\n      const dialog = document.createElement('mn-dialog');\n      this.searchSheet = dialog;\n      this.searchSheet.classList.add('search-sheet');\n      const input = document.createElement('mn-input');\n      input.setAttribute('placeholder', 'Type to search');\n\n      this.searchSheet.appendChild(input);\n      document.body.appendChild(this.searchSheet);\n\n      this.searchSheetInput = this.searchSheet.querySelector('mn-input');\n      this.setSearchSheetList();\n\n      this.searchSheetInput.addEventListener('input', () => {\n        this.filter = event.target.value;\n        const search = new Event('search');\n        search.query = event.target.value;\n        this.dispatchEvent(search);\n      });\n\n      this.input.addEventListener('focus', () => {\n        this.blur();\n        this.searchSheet.open();\n        this.searchSheetInput.value = '';\n        this.searchSheetInput.dispatchEvent(new Event('input'));\n        setTimeout(() => {\n          this.searchSheetInput.focus();\n        }, 410);\n      });\n    }\n  }\n\n  setSearchSheetList() {\n    this.searchSheetList = document.createElement('ul');\n    this.searchSheetList.classList.add('mn-list');\n    this.searchSheet.querySelector('.mn-card').appendChild(this.searchSheetList);\n\n    const observer = new MutationObserver(mutations => {\n      mutations.forEach(mutation => {\n        const addedNode = mutation.addedNodes[0];\n        const removedNode = mutation.removedNodes[0];\n        const addOption = addedNode && addedNode.tagName === 'OPTION';\n        const removeOption = removedNode && removedNode.tagName === 'OPTION';\n\n        if (addOption) {\n          const item = document.createElement('div');\n          item.classList.add('mn-item');\n          item.textContent = addedNode.textContent;\n          item.setAttribute('value', addedNode.getAttribute('value') || addedNode.textContent);\n          this.searchSheetList.appendChild(item);\n\n          item.addEventListener('touchend', event => {\n            this.searchSheet.close();\n            this.value = event.target.getAttribute('value');\n          });\n        }\n\n        if (removeOption) {\n          const value = removedNode.getAttribute('value');\n          const item = this.searchSheet.querySelector(`.mn-item[value=\"${value}\"]`);\n          item.parentNode.removeChild(item);\n        }\n      });\n    });\n\n    observer.observe(this, {\n      attributes: false,\n      childList: true,\n      characterData: false\n    });\n  }\n\n  setInput() {\n    super.setInput();\n\n    this.input.addEventListener('input', () => {\n      const event = new Event('search');\n      event.query = this.input.value;\n      this.dispatchEvent(event);\n    });\n  }\n\n  fetch(request) {\n    const requestType = typeof request;\n    const loader = requestType === 'function' ? request : () => fetch(request);\n\n    this.classList.add('loading');\n\n    return loader().then(res => {\n      this.cleanOptions();\n      this.classList.remove('loading');\n      this.dispatchEvent(new Event('loading'));\n\n      return res;\n    });\n  }\n}\n\nexports.default = MnSearch;\nwindow.customElements.define('mn-search', MnSearch);\n\n//# sourceURL=webpack:///./components/search/search.class.js?")},"./components/select/select.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _inputClass = __webpack_require__(/*! ../input/input.class.js */ \"./components/input/input.class.js\");\n\nvar _inputClass2 = _interopRequireDefault(_inputClass);\n\nvar _actionSheetClass = __webpack_require__(/*! ../action-sheet/action-sheet.class.js */ \"./components/action-sheet/action-sheet.class.js\");\n\nvar _actionSheetClass2 = _interopRequireDefault(_actionSheetClass);\n\nvar _evaluateString = __webpack_require__(/*! evaluate-string */ \"./node_modules/evaluate-string/index.js\");\n\nvar _evaluateString2 = _interopRequireDefault(_evaluateString);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnSelect extends _inputClass2.default {\n  constructor(self) {\n    self = super(self);\n    this.delimeterKeys = [];\n    return self;\n  }\n\n  connectedCallback() {\n    this.empty();\n    this.setStyle();\n    this.setInput();\n    super.setPlaceholder();\n    this._setMenu();\n    this._setActionSheet();\n    this._setOptions();\n    this._setKeyboardNavigation();\n    this.setAttributeValue();\n    super.setAttributeName();\n    super.setAttributeDisabled();\n    super.setAttributeReadonly();\n    super.setAttributeAutofocus();\n    super.setAttributeAutocomplete();\n    super.setAttributeSpellcheck();\n    this.setValidations();\n  }\n\n  disconnectedCallback() {\n    if (this.actionSheet) {\n      this.actionSheet.parentNode.removeChild(this.actionSheet);\n    }\n  }\n\n  static get observedAttributes() {\n    return ['value', 'name', 'placeholder', 'disabled', 'readonly', 'autofocus'];\n  }\n\n  empty() {\n    Array.from(this.children).forEach(children => {\n      if (children.tagName !== 'OPTION') {\n        this.removeChild(children);\n      }\n    });\n  }\n\n  cleanOptions() {\n    const options = this.querySelectorAll('option');\n    Array.from(options).forEach(option => this.removeChild(option));\n  }\n\n  _setOptions() {\n    Array.from(this.querySelectorAll('option')).forEach(option => {\n      const hasAngularAttribute = Array.from(option.attributes).some(attribute => attribute.name.startsWith('ng-'));\n\n      if (!hasAngularAttribute) {\n        this.addOption(option);\n      }\n    });\n\n    const observer = new MutationObserver(mutations => {\n      mutations.forEach(mutation => {\n        const addedNode = mutation.addedNodes[0];\n        const removedNode = mutation.removedNodes[0];\n        const addOption = addedNode && addedNode.tagName === 'OPTION';\n        const removeOption = removedNode && removedNode.tagName === 'OPTION';\n        if (addOption) {\n          this.addOption(addedNode);\n\n          const isObjectValue = typeof this.value === 'object' && typeof (0, _evaluateString2.default)(this.getAttribute('value')) === 'object';\n\n          const isOptionSelected = addedNode.getAttribute('value') === this.getAttribute('value') || addedNode.textContent === this.getAttribute('value') || isObjectValue && this.value.id ? this.value.id === (0, _evaluateString2.default)(addedNode.getAttribute('value')).id : false;\n\n          if (isOptionSelected && !this.classList.contains('focus')) {\n            this.input.value = addedNode.textContent;\n            this.classList.add('has-value');\n          }\n        }\n\n        if (removeOption) {\n          this.removeOption(removedNode);\n        }\n      });\n    });\n\n    observer.observe(this, {\n      attributes: false,\n      childList: true,\n      characterData: false\n    });\n\n    document.addEventListener('mousedown', event => {\n      const isOption = event.target.classList.contains('option') && event.target.closest('.mn-select') === this;\n\n      if (isOption) {\n        event.stopPropagation();\n        event.preventDefault();\n\n        const value = event.target.getAttribute('value') || event.target.textContent;\n        const text = event.target.textContent;\n\n        if (this.hasAttribute('multiple')) {\n          this.push(value, text);\n        } else {\n          this.value = value;\n          this.input.blur();\n        }\n      }\n    });\n\n    document.addEventListener('mousemove', event => {\n      const isOption = event.target.classList && event.target.classList.contains('option') && event.target.closest('.mn-select') === this;\n\n      if (isOption) {\n        if (!this.keyboardNavigation) {\n          this.focusOption(event.target);\n        }\n      }\n    });\n  }\n\n  addOption(value) {\n    const option = document.createElement('div');\n    option.classList.add('option');\n    option.innerHTML = value.textContent;\n    const focusedOption = this.menu.querySelector('.option.focus');\n\n    if (!focusedOption) {\n      option.classList.add('focus');\n    }\n\n    if (value.hasAttribute('selected')) {\n      option.setAttribute('selected', '');\n    }\n\n    option.innerHTML = value.textContent.split('').map(char => `<span class=\"char\" data-char=\"${char.toLowerCase()}\">${char}</span>`).join('');\n\n    if (value.hasAttribute('value')) {\n      option.setAttribute('value', value.getAttribute('value'));\n    }\n\n    this.menu.appendChild(option);\n\n    if (this.actionSheet) {\n      const actionSheetOption = document.createElement('div');\n      actionSheetOption.classList.add('option');\n      actionSheetOption.textContent = option.textContent;\n      this.actionSheet.menu.appendChild(actionSheetOption);\n    }\n\n    this.filter = this.filter;\n  }\n\n  removeOption(value) {\n    const option = Array.from(this.menu.querySelectorAll('.option')).find(option => option.textContent === value.textContent);\n\n    if (option) {\n      this.menu.removeChild(option);\n    }\n\n    if (this.actionSheet) {\n      const actionSheetOption = Array.from(this.actionSheet.menu.querySelectorAll('.option')).find(option => option.textContent === value.textContent);\n\n      if (actionSheetOption) {\n        this.actionSheet.menu.removeChild(actionSheetOption);\n      }\n    }\n  }\n\n  setStyle() {\n    super.setStyle();\n    this.classList.add('mn-select');\n  }\n\n  setInput() {\n    super.setInput();\n\n    this.input.addEventListener('focus', () => {\n      this.input.select();\n      !this.input.hasAttribute('readonly') ? this.show() : undefined;\n      this.filter = undefined;\n    });\n\n    this.input.addEventListener('blur', () => {\n      const option = Array.from(this.menu.querySelectorAll('.option')).filter(option => {\n        const optionValue = option.hasAttribute('value') ? option.getAttribute('value') : option.textContent;\n        return optionValue === this.getAttribute('value');\n      })[0];\n\n      if (this.input.value && option) {\n        this.input.value = option.textContent;\n      } else if (this.hasAttribute('multiple')) {\n        this.input.value = '';\n        this.filter = '';\n      } else {\n        this.value = undefined;\n      }\n      this.hide();\n    });\n\n    this.input.addEventListener('input', () => {\n      this.filter = this.input.value;\n      this.focusOption(this.menu.querySelector('.option:not(.hidden)'));\n    });\n\n    document.addEventListener('click', event => {\n      const clickOutside = !event.target.closest('.mn-select') && event.target !== this;\n\n      if (this.visible && clickOutside) {\n        this.hide();\n      }\n    });\n  }\n\n  setAttributeValue() {\n    Array.from(this.querySelectorAll('.option[selected]')).forEach(option => {\n      const value = option.getAttribute('value') || option.textContent;\n      this.hasAttribute('multiple') ? this.push(value, option.textContent) : this.value = value;\n    });\n  }\n\n  _setMenu() {\n    const menu = document.createElement('menu');\n    menu.classList.add('mn-card');\n\n    this.appendChild(menu);\n    this.menu = menu;\n  }\n\n  _setActionSheet() {\n    if (screen.width < 768) {\n      const actionSheet = new _actionSheetClass2.default();\n      this.actionSheet = actionSheet;\n      this.actionSheet.addEventListener('change', event => {\n        const { index } = event.data;\n        const option = this.menu.querySelector(`.option:nth-child(${index + 1})`);\n        const value = option.getAttribute('value') || option.textContent;\n\n        this.hasAttribute('multiple') ? this.push(value, option.textContent) : this.value = value;\n        this.actionSheet.hide();\n      });\n      document.body.appendChild(this.actionSheet);\n    }\n  }\n\n  _setKeyboardNavigation() {\n    this.input.addEventListener('keydown', event => {\n      // arrow navigate\n      const arrowDown = event.key === 'ArrowDown';\n      const arrowUp = event.key === 'ArrowUp';\n      let nextOption;\n\n      const options = Array.from(this.menu.querySelectorAll('.option:not(.hidden)'));\n      const currentOption = this.menu.querySelector('.option.focus');\n\n      const currentIndex = Array.prototype.indexOf.call(options, currentOption);\n\n      if (arrowDown) {\n        event.preventDefault();\n        nextOption = options[currentIndex + 1];\n      } else if (arrowUp) {\n        event.preventDefault();\n        nextOption = options[currentIndex - 1];\n      }\n\n      if (nextOption) {\n        const top = nextOption.offsetTop;\n        const bottom = top + nextOption.clientHeight;\n        const scrollToTop = top < this.menu.scrollTop;\n        const scrollToBottom = bottom > this.menu.scrollTop + this.menu.clientHeight;\n\n        this.keyboardNavigation = true;\n        if (scrollToTop) {\n          this.menu.scrollTop = top;\n        } else if (scrollToBottom) {\n          this.menu.scrollTop = bottom - this.menu.clientHeight;\n        }\n\n        this.focusOption(nextOption);\n        setTimeout(() => {\n          delete this.keyboardNavigation;\n        }, 100);\n      }\n    });\n\n    this.input.addEventListener('keydown', event => {\n      const enter = event.key === 'Enter';\n      const option = this.menu.querySelector('.option.focus');\n\n      if (enter) {\n        event.preventDefault();\n        event.stopPropagation();\n      }\n\n      if (enter && option) {\n        const value = option ? option.getAttribute('value') || option.textContent : this.value;\n\n        this.hasAttribute('multiple') ? this.push(value, option.textContent) : this.value = value;\n\n        if (this.hasAttribute('multiple')) {\n          this.input.value = '';\n        } else {\n          this.hide();\n          this.input.blur();\n        }\n      }\n    });\n\n    this.input.addEventListener('keydown', event => {\n      const esc = event.key === 'Escape';\n\n      if (esc) {\n        this.value = this.value;\n        this.input.select();\n        this.filter = undefined;\n      }\n    });\n  }\n\n  setValidations() {\n    super.setValidations();\n    this.validations.required = () => {\n      return this.hasAttribute('multiple') ? this.value.length === 0 : this.value === undefined;\n    };\n    delete this.validations.pattern;\n  }\n\n  show() {\n    this.classList.add('visible');\n    this.menu.scrollTop = 0;\n    this.focusOption(this.querySelector('.option:first-child'));\n\n    if (this.actionSheet) {\n      this.input.blur();\n      this.actionSheet.show();\n    }\n  }\n\n  hide() {\n    this.classList.remove('visible');\n    this.removeOptionFocus();\n  }\n\n  get visible() {\n    return this.classList.contains('visible');\n  }\n\n  removeOptionFocus() {\n    const latest = this.menu.querySelector('.focus');\n    latest ? latest.classList.remove('focus') : undefined;\n  }\n\n  focusOption(option) {\n    this.removeOptionFocus();\n    option ? option.classList.add('focus') : null;\n  }\n\n  get value() {\n    const value = this.getAttribute('value') ? (0, _evaluateString2.default)(this.getAttribute('value')) : undefined;\n\n    return this.hasAttribute('multiple') && !value ? [] : value;\n  }\n\n  set value(value) {\n    const valueIsMultiple = this.hasAttribute('multiple');\n    const differentValue = typeof value === 'object' ? this.getAttribute('value') !== JSON.stringify(value) : this.getAttribute('value') !== value;\n    const option = Array.from(this.querySelectorAll('option')).filter(option => {\n      return option.getAttribute('value') === String(value) || option.getAttribute('value') === JSON.stringify(value) || option.textContent === String(value);\n    })[0];\n\n    const textNotApplied = option && this.input.value !== option.textContent;\n\n    if (textNotApplied) {\n      this.input.value = option && !this.hasAttribute('multiple') ? option.textContent : '';\n\n      this.input.dispatchEvent(new Event('change'));\n    }\n\n    if (differentValue) {\n      const search = new Event('search');\n      search.query = typeof value === 'string' ? (0, _evaluateString2.default)(value) : '';\n\n      this.dispatchEvent(search);\n\n      const hasValue = value !== undefined && value !== null && value !== '' && value.length !== 0;\n\n      const optionValue = option ? option.hasAttribute('value') ? (0, _evaluateString2.default)(option.getAttribute('value')) : option.textContent : (0, _evaluateString2.default)(JSON.stringify(value));\n\n      hasValue ? this.setAttribute('value', typeof optionValue === 'object' ? JSON.stringify(optionValue) : optionValue) : this.removeAttribute('value');\n\n      if (valueIsMultiple) {\n        Array.from(this.querySelectorAll('.value')).forEach(item => item.parentNode.removeChild(item));\n\n        const values = Array.isArray((0, _evaluateString2.default)(value)) ? (0, _evaluateString2.default)(value) : [value].filter(item => item);\n\n        values.forEach(val => {\n          const option = Array.from(this.querySelectorAll('option')).filter(option => {\n            return option.getAttribute('value') === String(val) || option.getAttribute('value') === JSON.stringify(val) || option.textContent === String(val);\n          })[0];\n\n          const text = option ? option.textContent : undefined;\n          this.push(val, text);\n        });\n      }\n\n      this.input.dispatchEvent(new Event('change'));\n    }\n\n    if (!this.hasAttribute('value')) {\n      this.input.value = '';\n      this.input.dispatchEvent(new Event('change'));\n    }\n  }\n\n  get filter() {\n    return this.input.value;\n  }\n\n  set filter(value) {\n    if (value) {\n      this.classList.add('filtered');\n\n      try {\n        Array.from(this.menu.querySelectorAll('.option')).forEach(option => {\n          const matchOption = RegExp(value.split('').join('.*'), 'i').test(option.textContent);\n\n          Array.from(option.querySelectorAll('.match')).forEach(char => char.classList.remove('match'));\n\n          if (matchOption) {\n            option.classList.remove('hidden');\n\n            value.split('').forEach(char => {\n              const selector = `span[data-char=\"${char.toLowerCase()}\"]:not(.match)`;\n              const letter = option.querySelector(`.match ~ ${selector}`) || option.querySelector(selector);\n              letter ? letter.classList.add('match') : null;\n            });\n          } else {\n            option.classList.remove('focus');\n            option.classList.add('hidden');\n          }\n        });\n      } catch (error) {\n        console.log(error);\n      }\n    } else {\n      this.classList.remove('filtered');\n      Array.from(this.querySelectorAll('.option.hidden')).forEach(option => option.classList.remove('hidden'));\n    }\n  }\n}\n\nexports.default = MnSelect;\nwindow.customElements.define('mn-select', MnSelect);\n\n//# sourceURL=webpack:///./components/select/select.class.js?")},"./components/sidenav/sidenav.class.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nObject.defineProperty(exports, \"__esModule\", {\n  value: true\n});\n\nvar _backdropClass = __webpack_require__(/*! ../backdrop/backdrop.class.js */ \"./components/backdrop/backdrop.class.js\");\n\nvar _backdropClass2 = _interopRequireDefault(_backdropClass);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\nclass MnSidenav extends window.HTMLElement {\n  constructor(self) {\n    self = super(self);\n    return self;\n  }\n\n  connectedCallback() {\n    this.setStyle();\n    this._setOpenEvents();\n    this._setToggleEvents();\n    this._setCloseEvents();\n  }\n\n  setStyle() {\n    this.classList.add('mn-sidenav');\n    this.classList.add('mn-card');\n    document.body.classList.add('mn-backdrop');\n  }\n\n  _setOpenEvents() {\n    document.addEventListener('click', event => {\n      if (event.target.matches(`[open-sidenav=\"${this.id}\"]`)) {\n        this.open();\n        event.stopPropagation();\n      }\n    });\n  }\n\n  _setToggleEvents() {\n    document.addEventListener('click', event => {\n      if (event.target.matches(`[toggle-sidenav=\"${this.id}\"]`)) {\n        this.toggle();\n        event.stopPropagation();\n      }\n    });\n  }\n\n  _setCloseEvents() {\n    document.addEventListener('click', event => {\n      if (event.target.matches('[close-sidenav]')) {\n        this.close();\n        event.stopPropagation();\n      }\n    });\n\n    document.addEventListener('click', event => {\n      const clickOutside = !event.target.matches('[open-sidenav]') && !event.target.matches('[close-sidenav]') && !event.target.matches('[toggle-sidenav]') && !event.target.closest('mn-sidenav');\n      const sidebarVisible = this.classList.contains('visible');\n\n      if (clickOutside && sidebarVisible) {\n        this.close();\n      }\n    });\n\n    document.addEventListener('keyup', event => {\n      const esc = event.key === 'Escape';\n      const isOpened = this.classList.contains('visible');\n\n      if (esc && isOpened) {\n        this.close();\n      }\n    });\n  }\n\n  open() {\n    const fontSizeHTML = parseInt(window.getComputedStyle(document.body, null).getPropertyValue('font-size'));\n    const activeElement = this.querySelector('.active');\n    this.scrollTop = activeElement ? activeElement.offsetTop - fontSizeHTML * 1.5 : 0;\n    this.classList.add('visible');\n    document.body.classList.add('mn-sidenav-visible');\n    _backdropClass2.default.show();\n  }\n\n  close() {\n    document.body.classList.remove('mn-sidenav-visible');\n    this.classList.remove('visible');\n    _backdropClass2.default.hide();\n  }\n\n  toggle() {\n    this.classList.toggle('visible') ? this.open() : this.close();\n  }\n}\n\nexports.default = MnSidenav;\nwindow.customElements.define('mn-sidenav', MnSidenav);\n\n//# sourceURL=webpack:///./components/sidenav/sidenav.class.js?")},"./hotsite/app.js":function(module,exports,__webpack_require__){"use strict";eval('\n\n__webpack_require__(/*! @webcomponents/custom-elements */ "./node_modules/@webcomponents/custom-elements/custom-elements.min.js");\n\nvar _index = __webpack_require__(/*! ../index.js */ "./index.js");\n\nvar _index2 = _interopRequireDefault(_index);\n\nvar _inputReact = __webpack_require__(/*! ../components/input/input.react.jsx */ "./components/input/input.react.jsx");\n\nvar _inputReact2 = _interopRequireDefault(_inputReact);\n\nfunction _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }\n\n//# sourceURL=webpack:///./hotsite/app.js?')},"./index.js":function(module,exports,__webpack_require__){"use strict";eval('\n\nmodule.exports = {\n  input: __webpack_require__(/*! ./components/input/input.class.js */ "./components/input/input.class.js"),\n  email: __webpack_require__(/*! ./components/email/email.class.js */ "./components/email/email.class.js"),\n  password: __webpack_require__(/*! ./components/password/password.class.js */ "./components/password/password.class.js"),\n  hidden: __webpack_require__(/*! ./components/hidden/hidden.class.js */ "./components/hidden/hidden.class.js"),\n  number: __webpack_require__(/*! ./components/number/number.class.js */ "./components/number/number.class.js"),\n  date: __webpack_require__(/*! ./components/date/date.class.js */ "./components/date/date.class.js"),\n  backdrop: __webpack_require__(/*! ./components/backdrop/backdrop.class.js */ "./components/backdrop/backdrop.class.js"),\n  select: __webpack_require__(/*! ./components/select/select.class.js */ "./components/select/select.class.js"),\n  actionSheet: __webpack_require__(/*! ./components/action-sheet/action-sheet.class.js */ "./components/action-sheet/action-sheet.class.js"),\n  form: __webpack_require__(/*! ./components/form/form.class.js */ "./components/form/form.class.js"),\n  sidenav: __webpack_require__(/*! ./components/sidenav/sidenav.class.js */ "./components/sidenav/sidenav.class.js"),\n  checkbox: __webpack_require__(/*! ./components/checkbox/checkbox.class.js */ "./components/checkbox/checkbox.class.js"),\n  radio: __webpack_require__(/*! ./components/radio/radio.class.js */ "./components/radio/radio.class.js"),\n  dialog: __webpack_require__(/*! ./components/dialog/dialog.class.js */ "./components/dialog/dialog.class.js"),\n  button: __webpack_require__(/*! ./components/button/button.class.js */ "./components/button/button.class.js"),\n  search: __webpack_require__(/*! ./components/search/search.class.js */ "./components/search/search.class.js"),\n  list: __webpack_require__(/*! ./components/list/list.class.js */ "./components/list/list.class.js"),\n  code: __webpack_require__(/*! ./components/code/code.class.js */ "./components/code/code.class.js"),\n  image: __webpack_require__(/*! ./components/image/image.class.js */ "./components/image/image.class.js")\n};\n\n//# sourceURL=webpack:///./index.js?')},"./node_modules/@webcomponents/custom-elements/custom-elements.min.js":function(module,exports){eval('(function(){\n\'use strict\';var h=new function(){};var aa=new Set("annotation-xml color-profile font-face font-face-src font-face-uri font-face-format font-face-name missing-glyph".split(" "));function n(b){var a=aa.has(b);b=/^[a-z][.0-9_a-z]*-[\\-.0-9_a-z]*$/.test(b);return!a&&b}function p(b){var a=b.isConnected;if(void 0!==a)return a;for(;b&&!(b.__CE_isImportDocument||b instanceof Document);)b=b.parentNode||(window.ShadowRoot&&b instanceof ShadowRoot?b.host:void 0);return!(!b||!(b.__CE_isImportDocument||b instanceof Document))}\nfunction q(b,a){for(;a&&a!==b&&!a.nextSibling;)a=a.parentNode;return a&&a!==b?a.nextSibling:null}\nfunction t(b,a,c){c=c?c:new Set;for(var d=b;d;){if(d.nodeType===Node.ELEMENT_NODE){var e=d;a(e);var f=e.localName;if("link"===f&&"import"===e.getAttribute("rel")){d=e.import;if(d instanceof Node&&!c.has(d))for(c.add(d),d=d.firstChild;d;d=d.nextSibling)t(d,a,c);d=q(b,e);continue}else if("template"===f){d=q(b,e);continue}if(e=e.__CE_shadowRoot)for(e=e.firstChild;e;e=e.nextSibling)t(e,a,c)}d=d.firstChild?d.firstChild:q(b,d)}}function u(b,a,c){b[a]=c};function v(){this.a=new Map;this.o=new Map;this.f=[];this.b=!1}function ba(b,a,c){b.a.set(a,c);b.o.set(c.constructor,c)}function w(b,a){b.b=!0;b.f.push(a)}function x(b,a){b.b&&t(a,function(a){return y(b,a)})}function y(b,a){if(b.b&&!a.__CE_patched){a.__CE_patched=!0;for(var c=0;c<b.f.length;c++)b.f[c](a)}}function z(b,a){var c=[];t(a,function(b){return c.push(b)});for(a=0;a<c.length;a++){var d=c[a];1===d.__CE_state?b.connectedCallback(d):A(b,d)}}\nfunction B(b,a){var c=[];t(a,function(b){return c.push(b)});for(a=0;a<c.length;a++){var d=c[a];1===d.__CE_state&&b.disconnectedCallback(d)}}\nfunction C(b,a,c){c=c?c:{};var d=c.w||new Set,e=c.s||function(a){return A(b,a)},f=[];t(a,function(a){if("link"===a.localName&&"import"===a.getAttribute("rel")){var c=a.import;c instanceof Node&&(c.__CE_isImportDocument=!0,c.__CE_hasRegistry=!0);c&&"complete"===c.readyState?c.__CE_documentLoadHandled=!0:a.addEventListener("load",function(){var c=a.import;if(!c.__CE_documentLoadHandled){c.__CE_documentLoadHandled=!0;var f=new Set(d);f.delete(c);C(b,c,{w:f,s:e})}})}else f.push(a)},d);if(b.b)for(a=0;a<\nf.length;a++)y(b,f[a]);for(a=0;a<f.length;a++)e(f[a])}\nfunction A(b,a){if(void 0===a.__CE_state){var c=a.ownerDocument;if(c.defaultView||c.__CE_isImportDocument&&c.__CE_hasRegistry)if(c=b.a.get(a.localName)){c.constructionStack.push(a);var d=c.constructor;try{try{if(new d!==a)throw Error("The custom element constructor did not produce the element being upgraded.");}finally{c.constructionStack.pop()}}catch(m){throw a.__CE_state=2,m;}a.__CE_state=1;a.__CE_definition=c;if(c.attributeChangedCallback)for(c=c.observedAttributes,d=0;d<c.length;d++){var e=c[d],\nf=a.getAttribute(e);null!==f&&b.attributeChangedCallback(a,e,null,f,null)}p(a)&&b.connectedCallback(a)}}}v.prototype.connectedCallback=function(b){var a=b.__CE_definition;a.connectedCallback&&a.connectedCallback.call(b)};v.prototype.disconnectedCallback=function(b){var a=b.__CE_definition;a.disconnectedCallback&&a.disconnectedCallback.call(b)};\nv.prototype.attributeChangedCallback=function(b,a,c,d,e){var f=b.__CE_definition;f.attributeChangedCallback&&-1<f.observedAttributes.indexOf(a)&&f.attributeChangedCallback.call(b,a,c,d,e)};function D(b,a){this.c=b;this.a=a;this.b=void 0;C(this.c,this.a);"loading"===this.a.readyState&&(this.b=new MutationObserver(this.f.bind(this)),this.b.observe(this.a,{childList:!0,subtree:!0}))}function E(b){b.b&&b.b.disconnect()}D.prototype.f=function(b){var a=this.a.readyState;"interactive"!==a&&"complete"!==a||E(this);for(a=0;a<b.length;a++)for(var c=b[a].addedNodes,d=0;d<c.length;d++)C(this.c,c[d])};function ca(){var b=this;this.b=this.a=void 0;this.f=new Promise(function(a){b.b=a;b.a&&a(b.a)})}function F(b){if(b.a)throw Error("Already resolved.");b.a=void 0;b.b&&b.b(void 0)};function G(b){this.i=!1;this.c=b;this.m=new Map;this.j=function(b){return b()};this.g=!1;this.l=[];this.u=new D(b,document)}\nG.prototype.define=function(b,a){var c=this;if(!(a instanceof Function))throw new TypeError("Custom element constructors must be functions.");if(!n(b))throw new SyntaxError("The element name \'"+b+"\' is not valid.");if(this.c.a.get(b))throw Error("A custom element with name \'"+b+"\' has already been defined.");if(this.i)throw Error("A custom element is already being defined.");this.i=!0;var d,e,f,m,l;try{var g=function(b){var a=k[b];if(void 0!==a&&!(a instanceof Function))throw Error("The \'"+b+"\' callback must be a function.");\nreturn a},k=a.prototype;if(!(k instanceof Object))throw new TypeError("The custom element constructor\'s prototype is not an object.");d=g("connectedCallback");e=g("disconnectedCallback");f=g("adoptedCallback");m=g("attributeChangedCallback");l=a.observedAttributes||[]}catch(r){return}finally{this.i=!1}a={localName:b,constructor:a,connectedCallback:d,disconnectedCallback:e,adoptedCallback:f,attributeChangedCallback:m,observedAttributes:l,constructionStack:[]};ba(this.c,b,a);this.l.push(a);this.g||\n(this.g=!0,this.j(function(){return da(c)}))};function da(b){if(!1!==b.g){b.g=!1;for(var a=b.l,c=[],d=new Map,e=0;e<a.length;e++)d.set(a[e].localName,[]);C(b.c,document,{s:function(a){if(void 0===a.__CE_state){var e=a.localName,f=d.get(e);f?f.push(a):b.c.a.get(e)&&c.push(a)}}});for(e=0;e<c.length;e++)A(b.c,c[e]);for(;0<a.length;){for(var f=a.shift(),e=f.localName,f=d.get(f.localName),m=0;m<f.length;m++)A(b.c,f[m]);(e=b.m.get(e))&&F(e)}}}G.prototype.get=function(b){if(b=this.c.a.get(b))return b.constructor};\nG.prototype.whenDefined=function(b){if(!n(b))return Promise.reject(new SyntaxError("\'"+b+"\' is not a valid custom element name."));var a=this.m.get(b);if(a)return a.f;a=new ca;this.m.set(b,a);this.c.a.get(b)&&!this.l.some(function(a){return a.localName===b})&&F(a);return a.f};G.prototype.v=function(b){E(this.u);var a=this.j;this.j=function(c){return b(function(){return a(c)})}};window.CustomElementRegistry=G;G.prototype.define=G.prototype.define;G.prototype.get=G.prototype.get;\nG.prototype.whenDefined=G.prototype.whenDefined;G.prototype.polyfillWrapFlushCallback=G.prototype.v;var H=window.Document.prototype.createElement,ea=window.Document.prototype.createElementNS,fa=window.Document.prototype.importNode,ga=window.Document.prototype.prepend,ha=window.Document.prototype.append,ia=window.DocumentFragment.prototype.prepend,ja=window.DocumentFragment.prototype.append,I=window.Node.prototype.cloneNode,J=window.Node.prototype.appendChild,K=window.Node.prototype.insertBefore,L=window.Node.prototype.removeChild,M=window.Node.prototype.replaceChild,N=Object.getOwnPropertyDescriptor(window.Node.prototype,\n"textContent"),O=window.Element.prototype.attachShadow,P=Object.getOwnPropertyDescriptor(window.Element.prototype,"innerHTML"),Q=window.Element.prototype.getAttribute,R=window.Element.prototype.setAttribute,S=window.Element.prototype.removeAttribute,T=window.Element.prototype.getAttributeNS,U=window.Element.prototype.setAttributeNS,ka=window.Element.prototype.removeAttributeNS,la=window.Element.prototype.insertAdjacentElement,ma=window.Element.prototype.prepend,na=window.Element.prototype.append,\nV=window.Element.prototype.before,oa=window.Element.prototype.after,pa=window.Element.prototype.replaceWith,qa=window.Element.prototype.remove,ra=window.HTMLElement,W=Object.getOwnPropertyDescriptor(window.HTMLElement.prototype,"innerHTML"),sa=window.HTMLElement.prototype.insertAdjacentElement;function ta(){var b=X;window.HTMLElement=function(){function a(){var a=this.constructor,d=b.o.get(a);if(!d)throw Error("The custom element being constructed was not registered with `customElements`.");var e=d.constructionStack;if(!e.length)return e=H.call(document,d.localName),Object.setPrototypeOf(e,a.prototype),e.__CE_state=1,e.__CE_definition=d,y(b,e),e;var d=e.length-1,f=e[d];if(f===h)throw Error("The HTMLElement constructor was either called reentrantly for this constructor or called multiple times.");\ne[d]=h;Object.setPrototypeOf(f,a.prototype);y(b,f);return f}a.prototype=ra.prototype;return a}()};function Y(b,a,c){function d(a){return function(d){for(var c=[],e=0;e<arguments.length;++e)c[e-0]=arguments[e];for(var e=[],f=[],k=0;k<c.length;k++){var r=c[k];r instanceof Element&&p(r)&&f.push(r);if(r instanceof DocumentFragment)for(r=r.firstChild;r;r=r.nextSibling)e.push(r);else e.push(r)}a.apply(this,c);for(c=0;c<f.length;c++)B(b,f[c]);if(p(this))for(c=0;c<e.length;c++)f=e[c],f instanceof Element&&z(b,f)}}c.h&&(a.prepend=d(c.h));c.append&&(a.append=d(c.append))};function ua(){var b=X;u(Document.prototype,"createElement",function(a){if(this.__CE_hasRegistry){var c=b.a.get(a);if(c)return new c.constructor}a=H.call(this,a);y(b,a);return a});u(Document.prototype,"importNode",function(a,c){a=fa.call(this,a,c);this.__CE_hasRegistry?C(b,a):x(b,a);return a});u(Document.prototype,"createElementNS",function(a,c){if(this.__CE_hasRegistry&&(null===a||"http://www.w3.org/1999/xhtml"===a)){var d=b.a.get(c);if(d)return new d.constructor}a=ea.call(this,a,c);y(b,a);return a});\nY(b,Document.prototype,{h:ga,append:ha})};function va(){var b=X;function a(a,d){Object.defineProperty(a,"textContent",{enumerable:d.enumerable,configurable:!0,get:d.get,set:function(a){if(this.nodeType===Node.TEXT_NODE)d.set.call(this,a);else{var c=void 0;if(this.firstChild){var e=this.childNodes,l=e.length;if(0<l&&p(this))for(var c=Array(l),g=0;g<l;g++)c[g]=e[g]}d.set.call(this,a);if(c)for(a=0;a<c.length;a++)B(b,c[a])}}})}u(Node.prototype,"insertBefore",function(a,d){if(a instanceof DocumentFragment){var c=Array.prototype.slice.apply(a.childNodes);\na=K.call(this,a,d);if(p(this))for(d=0;d<c.length;d++)z(b,c[d]);return a}c=p(a);d=K.call(this,a,d);c&&B(b,a);p(this)&&z(b,a);return d});u(Node.prototype,"appendChild",function(a){if(a instanceof DocumentFragment){var c=Array.prototype.slice.apply(a.childNodes);a=J.call(this,a);if(p(this))for(var e=0;e<c.length;e++)z(b,c[e]);return a}c=p(a);e=J.call(this,a);c&&B(b,a);p(this)&&z(b,a);return e});u(Node.prototype,"cloneNode",function(a){a=I.call(this,a);this.ownerDocument.__CE_hasRegistry?C(b,a):x(b,a);\nreturn a});u(Node.prototype,"removeChild",function(a){var c=p(a),e=L.call(this,a);c&&B(b,a);return e});u(Node.prototype,"replaceChild",function(a,d){if(a instanceof DocumentFragment){var e=Array.prototype.slice.apply(a.childNodes);a=M.call(this,a,d);if(p(this))for(B(b,d),d=0;d<e.length;d++)z(b,e[d]);return a}var e=p(a),c=M.call(this,a,d),m=p(this);m&&B(b,d);e&&B(b,a);m&&z(b,a);return c});N&&N.get?a(Node.prototype,N):w(b,function(b){a(b,{enumerable:!0,configurable:!0,get:function(){for(var a=[],b=\n0;b<this.childNodes.length;b++)a.push(this.childNodes[b].textContent);return a.join("")},set:function(a){for(;this.firstChild;)L.call(this,this.firstChild);J.call(this,document.createTextNode(a))}})})};function wa(b){var a=Element.prototype;function c(a){return function(c){for(var d=[],e=0;e<arguments.length;++e)d[e-0]=arguments[e];for(var e=[],l=[],g=0;g<d.length;g++){var k=d[g];k instanceof Element&&p(k)&&l.push(k);if(k instanceof DocumentFragment)for(k=k.firstChild;k;k=k.nextSibling)e.push(k);else e.push(k)}a.apply(this,d);for(d=0;d<l.length;d++)B(b,l[d]);if(p(this))for(d=0;d<e.length;d++)l=e[d],l instanceof Element&&z(b,l)}}V&&(a.before=c(V));V&&(a.after=c(oa));pa&&u(a,"replaceWith",function(a){for(var d=\n[],c=0;c<arguments.length;++c)d[c-0]=arguments[c];for(var c=[],m=[],l=0;l<d.length;l++){var g=d[l];g instanceof Element&&p(g)&&m.push(g);if(g instanceof DocumentFragment)for(g=g.firstChild;g;g=g.nextSibling)c.push(g);else c.push(g)}l=p(this);pa.apply(this,d);for(d=0;d<m.length;d++)B(b,m[d]);if(l)for(B(b,this),d=0;d<c.length;d++)m=c[d],m instanceof Element&&z(b,m)});qa&&u(a,"remove",function(){var a=p(this);qa.call(this);a&&B(b,this)})};function xa(){var b=X;function a(a,c){Object.defineProperty(a,"innerHTML",{enumerable:c.enumerable,configurable:!0,get:c.get,set:function(a){var d=this,e=void 0;p(this)&&(e=[],t(this,function(a){a!==d&&e.push(a)}));c.set.call(this,a);if(e)for(var f=0;f<e.length;f++){var k=e[f];1===k.__CE_state&&b.disconnectedCallback(k)}this.ownerDocument.__CE_hasRegistry?C(b,this):x(b,this);return a}})}function c(a,c){u(a,"insertAdjacentElement",function(a,d){var e=p(d);a=c.call(this,a,d);e&&B(b,d);p(a)&&z(b,d);\nreturn a})}O&&u(Element.prototype,"attachShadow",function(a){return this.__CE_shadowRoot=a=O.call(this,a)});P&&P.get?a(Element.prototype,P):W&&W.get?a(HTMLElement.prototype,W):w(b,function(b){a(b,{enumerable:!0,configurable:!0,get:function(){return I.call(this,!0).innerHTML},set:function(a){var b="template"===this.localName,d=b?this.content:this,c=H.call(document,this.localName);for(c.innerHTML=a;0<d.childNodes.length;)L.call(d,d.childNodes[0]);for(a=b?c.content:c;0<a.childNodes.length;)J.call(d,\na.childNodes[0])}})});u(Element.prototype,"setAttribute",function(a,c){if(1!==this.__CE_state)return R.call(this,a,c);var d=Q.call(this,a);R.call(this,a,c);c=Q.call(this,a);b.attributeChangedCallback(this,a,d,c,null)});u(Element.prototype,"setAttributeNS",function(a,c,f){if(1!==this.__CE_state)return U.call(this,a,c,f);var d=T.call(this,a,c);U.call(this,a,c,f);f=T.call(this,a,c);b.attributeChangedCallback(this,c,d,f,a)});u(Element.prototype,"removeAttribute",function(a){if(1!==this.__CE_state)return S.call(this,\na);var c=Q.call(this,a);S.call(this,a);null!==c&&b.attributeChangedCallback(this,a,c,null,null)});u(Element.prototype,"removeAttributeNS",function(a,c){if(1!==this.__CE_state)return ka.call(this,a,c);var d=T.call(this,a,c);ka.call(this,a,c);var e=T.call(this,a,c);d!==e&&b.attributeChangedCallback(this,c,d,e,a)});sa?c(HTMLElement.prototype,sa):la?c(Element.prototype,la):console.warn("Custom Elements: `Element#insertAdjacentElement` was not patched.");Y(b,Element.prototype,{h:ma,append:na});wa(b)};/*\n\n Copyright (c) 2016 The Polymer Project Authors. All rights reserved.\n This code may only be used under the BSD style license found at http://polymer.github.io/LICENSE.txt\n The complete set of authors may be found at http://polymer.github.io/AUTHORS.txt\n The complete set of contributors may be found at http://polymer.github.io/CONTRIBUTORS.txt\n Code distributed by Google as part of the polymer project is also\n subject to an additional IP rights grant found at http://polymer.github.io/PATENTS.txt\n*/\nvar Z=window.customElements;if(!Z||Z.forcePolyfill||"function"!=typeof Z.define||"function"!=typeof Z.get){var X=new v;ta();ua();Y(X,DocumentFragment.prototype,{h:ia,append:ja});va();xa();document.__CE_hasRegistry=!0;var customElements=new G(X);Object.defineProperty(window,"customElements",{configurable:!0,enumerable:!0,value:customElements})};\n}).call(self);\n\n//# sourceMappingURL=custom-elements.min.js.map\n\n\n//# sourceURL=webpack:///./node_modules/@webcomponents/custom-elements/custom-elements.min.js?')},"./node_modules/atoa/atoa.js":function(module,exports){eval("module.exports = function atoa (a, n) { return Array.prototype.slice.call(a, n); }\n\n\n//# sourceURL=webpack:///./node_modules/atoa/atoa.js?")},"./node_modules/contra/debounce.js":function(module,exports,__webpack_require__){"use strict";eval('\n\nvar ticky = __webpack_require__(/*! ticky */ "./node_modules/ticky/ticky-browser.js");\n\nmodule.exports = function debounce (fn, args, ctx) {\n  if (!fn) { return; }\n  ticky(function run () {\n    fn.apply(ctx || null, args || []);\n  });\n};\n\n\n//# sourceURL=webpack:///./node_modules/contra/debounce.js?')},"./node_modules/contra/emitter.js":function(module,exports,__webpack_require__){"use strict";eval('\n\nvar atoa = __webpack_require__(/*! atoa */ "./node_modules/atoa/atoa.js");\nvar debounce = __webpack_require__(/*! ./debounce */ "./node_modules/contra/debounce.js");\n\nmodule.exports = function emitter (thing, options) {\n  var opts = options || {};\n  var evt = {};\n  if (thing === undefined) { thing = {}; }\n  thing.on = function (type, fn) {\n    if (!evt[type]) {\n      evt[type] = [fn];\n    } else {\n      evt[type].push(fn);\n    }\n    return thing;\n  };\n  thing.once = function (type, fn) {\n    fn._once = true; // thing.off(fn) still works!\n    thing.on(type, fn);\n    return thing;\n  };\n  thing.off = function (type, fn) {\n    var c = arguments.length;\n    if (c === 1) {\n      delete evt[type];\n    } else if (c === 0) {\n      evt = {};\n    } else {\n      var et = evt[type];\n      if (!et) { return thing; }\n      et.splice(et.indexOf(fn), 1);\n    }\n    return thing;\n  };\n  thing.emit = function () {\n    var args = atoa(arguments);\n    return thing.emitterSnapshot(args.shift()).apply(this, args);\n  };\n  thing.emitterSnapshot = function (type) {\n    var et = (evt[type] || []).slice(0);\n    return function () {\n      var args = atoa(arguments);\n      var ctx = this || thing;\n      if (type === \'error\' && opts.throws !== false && !et.length) { throw args.length === 1 ? args[0] : args; }\n      et.forEach(function emitter (listen) {\n        if (opts.async) { debounce(listen, args, ctx); } else { listen.apply(ctx, args); }\n        if (listen._once) { thing.off(type, listen); }\n      });\n      return thing;\n    };\n  };\n  return thing;\n};\n\n\n//# sourceURL=webpack:///./node_modules/contra/emitter.js?')},"./node_modules/crossvent/src/crossvent.js":function(module,exports,__webpack_require__){"use strict";eval("/* WEBPACK VAR INJECTION */(function(global) {\n\nvar customEvent = __webpack_require__(/*! custom-event */ \"./node_modules/custom-event/index.js\");\nvar eventmap = __webpack_require__(/*! ./eventmap */ \"./node_modules/crossvent/src/eventmap.js\");\nvar doc = global.document;\nvar addEvent = addEventEasy;\nvar removeEvent = removeEventEasy;\nvar hardCache = [];\n\nif (!global.addEventListener) {\n  addEvent = addEventHard;\n  removeEvent = removeEventHard;\n}\n\nmodule.exports = {\n  add: addEvent,\n  remove: removeEvent,\n  fabricate: fabricateEvent\n};\n\nfunction addEventEasy (el, type, fn, capturing) {\n  return el.addEventListener(type, fn, capturing);\n}\n\nfunction addEventHard (el, type, fn) {\n  return el.attachEvent('on' + type, wrap(el, type, fn));\n}\n\nfunction removeEventEasy (el, type, fn, capturing) {\n  return el.removeEventListener(type, fn, capturing);\n}\n\nfunction removeEventHard (el, type, fn) {\n  var listener = unwrap(el, type, fn);\n  if (listener) {\n    return el.detachEvent('on' + type, listener);\n  }\n}\n\nfunction fabricateEvent (el, type, model) {\n  var e = eventmap.indexOf(type) === -1 ? makeCustomEvent() : makeClassicEvent();\n  if (el.dispatchEvent) {\n    el.dispatchEvent(e);\n  } else {\n    el.fireEvent('on' + type, e);\n  }\n  function makeClassicEvent () {\n    var e;\n    if (doc.createEvent) {\n      e = doc.createEvent('Event');\n      e.initEvent(type, true, true);\n    } else if (doc.createEventObject) {\n      e = doc.createEventObject();\n    }\n    return e;\n  }\n  function makeCustomEvent () {\n    return new customEvent(type, { detail: model });\n  }\n}\n\nfunction wrapperFactory (el, type, fn) {\n  return function wrapper (originalEvent) {\n    var e = originalEvent || global.event;\n    e.target = e.target || e.srcElement;\n    e.preventDefault = e.preventDefault || function preventDefault () { e.returnValue = false; };\n    e.stopPropagation = e.stopPropagation || function stopPropagation () { e.cancelBubble = true; };\n    e.which = e.which || e.keyCode;\n    fn.call(el, e);\n  };\n}\n\nfunction wrap (el, type, fn) {\n  var wrapper = unwrap(el, type, fn) || wrapperFactory(el, type, fn);\n  hardCache.push({\n    wrapper: wrapper,\n    element: el,\n    type: type,\n    fn: fn\n  });\n  return wrapper;\n}\n\nfunction unwrap (el, type, fn) {\n  var i = find(el, type, fn);\n  if (i) {\n    var wrapper = hardCache[i].wrapper;\n    hardCache.splice(i, 1); // free up a tad of memory\n    return wrapper;\n  }\n}\n\nfunction find (el, type, fn) {\n  var i, item;\n  for (i = 0; i < hardCache.length; i++) {\n    item = hardCache[i];\n    if (item.element === el && item.type === type && item.fn === fn) {\n      return i;\n    }\n  }\n}\n\n/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../../webpack/buildin/global.js */ \"./node_modules/webpack/buildin/global.js\")))\n\n//# sourceURL=webpack:///./node_modules/crossvent/src/crossvent.js?")},"./node_modules/crossvent/src/eventmap.js":function(module,exports,__webpack_require__){"use strict";eval("/* WEBPACK VAR INJECTION */(function(global) {\n\nvar eventmap = [];\nvar eventname = '';\nvar ron = /^on/;\n\nfor (eventname in global) {\n  if (ron.test(eventname)) {\n    eventmap.push(eventname.slice(2));\n  }\n}\n\nmodule.exports = eventmap;\n\n/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../../webpack/buildin/global.js */ \"./node_modules/webpack/buildin/global.js\")))\n\n//# sourceURL=webpack:///./node_modules/crossvent/src/eventmap.js?")},"./node_modules/custom-event/index.js":function(module,exports,__webpack_require__){eval("/* WEBPACK VAR INJECTION */(function(global) {\nvar NativeCustomEvent = global.CustomEvent;\n\nfunction useNative () {\n  try {\n    var p = new NativeCustomEvent('cat', { detail: { foo: 'bar' } });\n    return  'cat' === p.type && 'bar' === p.detail.foo;\n  } catch (e) {\n  }\n  return false;\n}\n\n/**\n * Cross-browser `CustomEvent` constructor.\n *\n * https://developer.mozilla.org/en-US/docs/Web/API/CustomEvent.CustomEvent\n *\n * @public\n */\n\nmodule.exports = useNative() ? NativeCustomEvent :\n\n// IE >= 9\n'function' === typeof document.createEvent ? function CustomEvent (type, params) {\n  var e = document.createEvent('CustomEvent');\n  if (params) {\n    e.initCustomEvent(type, params.bubbles, params.cancelable, params.detail);\n  } else {\n    e.initCustomEvent(type, false, false, void 0);\n  }\n  return e;\n} :\n\n// IE <= 8\nfunction CustomEvent (type, params) {\n  var e = document.createEventObject();\n  e.type = type;\n  if (params) {\n    e.bubbles = Boolean(params.bubbles);\n    e.cancelable = Boolean(params.cancelable);\n    e.detail = params.detail;\n  } else {\n    e.bubbles = false;\n    e.cancelable = false;\n    e.detail = void 0;\n  }\n  return e;\n}\n\n/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../webpack/buildin/global.js */ \"./node_modules/webpack/buildin/global.js\")))\n\n//# sourceURL=webpack:///./node_modules/custom-event/index.js?")},"./node_modules/dragula/classes.js":function(module,exports,__webpack_require__){"use strict";eval("\n\nvar cache = {};\nvar start = '(?:^|\\\\s)';\nvar end = '(?:\\\\s|$)';\n\nfunction lookupClass (className) {\n  var cached = cache[className];\n  if (cached) {\n    cached.lastIndex = 0;\n  } else {\n    cache[className] = cached = new RegExp(start + className + end, 'g');\n  }\n  return cached;\n}\n\nfunction addClass (el, className) {\n  var current = el.className;\n  if (!current.length) {\n    el.className = className;\n  } else if (!lookupClass(className).test(current)) {\n    el.className += ' ' + className;\n  }\n}\n\nfunction rmClass (el, className) {\n  el.className = el.className.replace(lookupClass(className), ' ').trim();\n}\n\nmodule.exports = {\n  add: addClass,\n  rm: rmClass\n};\n\n\n//# sourceURL=webpack:///./node_modules/dragula/classes.js?")},"./node_modules/dragula/dragula.js":function(module,exports,__webpack_require__){"use strict";eval("/* WEBPACK VAR INJECTION */(function(global) {\n\nvar emitter = __webpack_require__(/*! contra/emitter */ \"./node_modules/contra/emitter.js\");\nvar crossvent = __webpack_require__(/*! crossvent */ \"./node_modules/crossvent/src/crossvent.js\");\nvar classes = __webpack_require__(/*! ./classes */ \"./node_modules/dragula/classes.js\");\nvar doc = document;\nvar documentElement = doc.documentElement;\n\nfunction dragula (initialContainers, options) {\n  var len = arguments.length;\n  if (len === 1 && Array.isArray(initialContainers) === false) {\n    options = initialContainers;\n    initialContainers = [];\n  }\n  var _mirror; // mirror image\n  var _source; // source container\n  var _item; // item being dragged\n  var _offsetX; // reference x\n  var _offsetY; // reference y\n  var _moveX; // reference move x\n  var _moveY; // reference move y\n  var _initialSibling; // reference sibling when grabbed\n  var _currentSibling; // reference sibling now\n  var _copy; // item used for copying\n  var _renderTimer; // timer for setTimeout renderMirrorImage\n  var _lastDropTarget = null; // last container item was over\n  var _grabbed; // holds mousedown context until first mousemove\n\n  var o = options || {};\n  if (o.moves === void 0) { o.moves = always; }\n  if (o.accepts === void 0) { o.accepts = always; }\n  if (o.invalid === void 0) { o.invalid = invalidTarget; }\n  if (o.containers === void 0) { o.containers = initialContainers || []; }\n  if (o.isContainer === void 0) { o.isContainer = never; }\n  if (o.copy === void 0) { o.copy = false; }\n  if (o.copySortSource === void 0) { o.copySortSource = false; }\n  if (o.revertOnSpill === void 0) { o.revertOnSpill = false; }\n  if (o.removeOnSpill === void 0) { o.removeOnSpill = false; }\n  if (o.direction === void 0) { o.direction = 'vertical'; }\n  if (o.ignoreInputTextSelection === void 0) { o.ignoreInputTextSelection = true; }\n  if (o.mirrorContainer === void 0) { o.mirrorContainer = doc.body; }\n\n  var drake = emitter({\n    containers: o.containers,\n    start: manualStart,\n    end: end,\n    cancel: cancel,\n    remove: remove,\n    destroy: destroy,\n    canMove: canMove,\n    dragging: false\n  });\n\n  if (o.removeOnSpill === true) {\n    drake.on('over', spillOver).on('out', spillOut);\n  }\n\n  events();\n\n  return drake;\n\n  function isContainer (el) {\n    return drake.containers.indexOf(el) !== -1 || o.isContainer(el);\n  }\n\n  function events (remove) {\n    var op = remove ? 'remove' : 'add';\n    touchy(documentElement, op, 'mousedown', grab);\n    touchy(documentElement, op, 'mouseup', release);\n  }\n\n  function eventualMovements (remove) {\n    var op = remove ? 'remove' : 'add';\n    touchy(documentElement, op, 'mousemove', startBecauseMouseMoved);\n  }\n\n  function movements (remove) {\n    var op = remove ? 'remove' : 'add';\n    crossvent[op](documentElement, 'selectstart', preventGrabbed); // IE8\n    crossvent[op](documentElement, 'click', preventGrabbed);\n  }\n\n  function destroy () {\n    events(true);\n    release({});\n  }\n\n  function preventGrabbed (e) {\n    if (_grabbed) {\n      e.preventDefault();\n    }\n  }\n\n  function grab (e) {\n    _moveX = e.clientX;\n    _moveY = e.clientY;\n\n    var ignore = whichMouseButton(e) !== 1 || e.metaKey || e.ctrlKey;\n    if (ignore) {\n      return; // we only care about honest-to-god left clicks and touch events\n    }\n    var item = e.target;\n    var context = canStart(item);\n    if (!context) {\n      return;\n    }\n    _grabbed = context;\n    eventualMovements();\n    if (e.type === 'mousedown') {\n      if (isInput(item)) { // see also: https://github.com/bevacqua/dragula/issues/208\n        item.focus(); // fixes https://github.com/bevacqua/dragula/issues/176\n      } else {\n        e.preventDefault(); // fixes https://github.com/bevacqua/dragula/issues/155\n      }\n    }\n  }\n\n  function startBecauseMouseMoved (e) {\n    if (!_grabbed) {\n      return;\n    }\n    if (whichMouseButton(e) === 0) {\n      release({});\n      return; // when text is selected on an input and then dragged, mouseup doesn't fire. this is our only hope\n    }\n    // truthy check fixes #239, equality fixes #207\n    if (e.clientX !== void 0 && e.clientX === _moveX && e.clientY !== void 0 && e.clientY === _moveY) {\n      return;\n    }\n    if (o.ignoreInputTextSelection) {\n      var clientX = getCoord('clientX', e);\n      var clientY = getCoord('clientY', e);\n      var elementBehindCursor = doc.elementFromPoint(clientX, clientY);\n      if (isInput(elementBehindCursor)) {\n        return;\n      }\n    }\n\n    var grabbed = _grabbed; // call to end() unsets _grabbed\n    eventualMovements(true);\n    movements();\n    end();\n    start(grabbed);\n\n    var offset = getOffset(_item);\n    _offsetX = getCoord('pageX', e) - offset.left;\n    _offsetY = getCoord('pageY', e) - offset.top;\n\n    classes.add(_copy || _item, 'gu-transit');\n    renderMirrorImage();\n    drag(e);\n  }\n\n  function canStart (item) {\n    if (drake.dragging && _mirror) {\n      return;\n    }\n    if (isContainer(item)) {\n      return; // don't drag container itself\n    }\n    var handle = item;\n    while (getParent(item) && isContainer(getParent(item)) === false) {\n      if (o.invalid(item, handle)) {\n        return;\n      }\n      item = getParent(item); // drag target should be a top element\n      if (!item) {\n        return;\n      }\n    }\n    var source = getParent(item);\n    if (!source) {\n      return;\n    }\n    if (o.invalid(item, handle)) {\n      return;\n    }\n\n    var movable = o.moves(item, source, handle, nextEl(item));\n    if (!movable) {\n      return;\n    }\n\n    return {\n      item: item,\n      source: source\n    };\n  }\n\n  function canMove (item) {\n    return !!canStart(item);\n  }\n\n  function manualStart (item) {\n    var context = canStart(item);\n    if (context) {\n      start(context);\n    }\n  }\n\n  function start (context) {\n    if (isCopy(context.item, context.source)) {\n      _copy = context.item.cloneNode(true);\n      drake.emit('cloned', _copy, context.item, 'copy');\n    }\n\n    _source = context.source;\n    _item = context.item;\n    _initialSibling = _currentSibling = nextEl(context.item);\n\n    drake.dragging = true;\n    drake.emit('drag', _item, _source);\n  }\n\n  function invalidTarget () {\n    return false;\n  }\n\n  function end () {\n    if (!drake.dragging) {\n      return;\n    }\n    var item = _copy || _item;\n    drop(item, getParent(item));\n  }\n\n  function ungrab () {\n    _grabbed = false;\n    eventualMovements(true);\n    movements(true);\n  }\n\n  function release (e) {\n    ungrab();\n\n    if (!drake.dragging) {\n      return;\n    }\n    var item = _copy || _item;\n    var clientX = getCoord('clientX', e);\n    var clientY = getCoord('clientY', e);\n    var elementBehindCursor = getElementBehindPoint(_mirror, clientX, clientY);\n    var dropTarget = findDropTarget(elementBehindCursor, clientX, clientY);\n    if (dropTarget && ((_copy && o.copySortSource) || (!_copy || dropTarget !== _source))) {\n      drop(item, dropTarget);\n    } else if (o.removeOnSpill) {\n      remove();\n    } else {\n      cancel();\n    }\n  }\n\n  function drop (item, target) {\n    var parent = getParent(item);\n    if (_copy && o.copySortSource && target === _source) {\n      parent.removeChild(_item);\n    }\n    if (isInitialPlacement(target)) {\n      drake.emit('cancel', item, _source, _source);\n    } else {\n      drake.emit('drop', item, target, _source, _currentSibling);\n    }\n    cleanup();\n  }\n\n  function remove () {\n    if (!drake.dragging) {\n      return;\n    }\n    var item = _copy || _item;\n    var parent = getParent(item);\n    if (parent) {\n      parent.removeChild(item);\n    }\n    drake.emit(_copy ? 'cancel' : 'remove', item, parent, _source);\n    cleanup();\n  }\n\n  function cancel (revert) {\n    if (!drake.dragging) {\n      return;\n    }\n    var reverts = arguments.length > 0 ? revert : o.revertOnSpill;\n    var item = _copy || _item;\n    var parent = getParent(item);\n    var initial = isInitialPlacement(parent);\n    if (initial === false && reverts) {\n      if (_copy) {\n        if (parent) {\n          parent.removeChild(_copy);\n        }\n      } else {\n        _source.insertBefore(item, _initialSibling);\n      }\n    }\n    if (initial || reverts) {\n      drake.emit('cancel', item, _source, _source);\n    } else {\n      drake.emit('drop', item, parent, _source, _currentSibling);\n    }\n    cleanup();\n  }\n\n  function cleanup () {\n    var item = _copy || _item;\n    ungrab();\n    removeMirrorImage();\n    if (item) {\n      classes.rm(item, 'gu-transit');\n    }\n    if (_renderTimer) {\n      clearTimeout(_renderTimer);\n    }\n    drake.dragging = false;\n    if (_lastDropTarget) {\n      drake.emit('out', item, _lastDropTarget, _source);\n    }\n    drake.emit('dragend', item);\n    _source = _item = _copy = _initialSibling = _currentSibling = _renderTimer = _lastDropTarget = null;\n  }\n\n  function isInitialPlacement (target, s) {\n    var sibling;\n    if (s !== void 0) {\n      sibling = s;\n    } else if (_mirror) {\n      sibling = _currentSibling;\n    } else {\n      sibling = nextEl(_copy || _item);\n    }\n    return target === _source && sibling === _initialSibling;\n  }\n\n  function findDropTarget (elementBehindCursor, clientX, clientY) {\n    var target = elementBehindCursor;\n    while (target && !accepted()) {\n      target = getParent(target);\n    }\n    return target;\n\n    function accepted () {\n      var droppable = isContainer(target);\n      if (droppable === false) {\n        return false;\n      }\n\n      var immediate = getImmediateChild(target, elementBehindCursor);\n      var reference = getReference(target, immediate, clientX, clientY);\n      var initial = isInitialPlacement(target, reference);\n      if (initial) {\n        return true; // should always be able to drop it right back where it was\n      }\n      return o.accepts(_item, target, _source, reference);\n    }\n  }\n\n  function drag (e) {\n    if (!_mirror) {\n      return;\n    }\n    e.preventDefault();\n\n    var clientX = getCoord('clientX', e);\n    var clientY = getCoord('clientY', e);\n    var x = clientX - _offsetX;\n    var y = clientY - _offsetY;\n\n    _mirror.style.left = x + 'px';\n    _mirror.style.top = y + 'px';\n\n    var item = _copy || _item;\n    var elementBehindCursor = getElementBehindPoint(_mirror, clientX, clientY);\n    var dropTarget = findDropTarget(elementBehindCursor, clientX, clientY);\n    var changed = dropTarget !== null && dropTarget !== _lastDropTarget;\n    if (changed || dropTarget === null) {\n      out();\n      _lastDropTarget = dropTarget;\n      over();\n    }\n    var parent = getParent(item);\n    if (dropTarget === _source && _copy && !o.copySortSource) {\n      if (parent) {\n        parent.removeChild(item);\n      }\n      return;\n    }\n    var reference;\n    var immediate = getImmediateChild(dropTarget, elementBehindCursor);\n    if (immediate !== null) {\n      reference = getReference(dropTarget, immediate, clientX, clientY);\n    } else if (o.revertOnSpill === true && !_copy) {\n      reference = _initialSibling;\n      dropTarget = _source;\n    } else {\n      if (_copy && parent) {\n        parent.removeChild(item);\n      }\n      return;\n    }\n    if (\n      (reference === null && changed) ||\n      reference !== item &&\n      reference !== nextEl(item)\n    ) {\n      _currentSibling = reference;\n      dropTarget.insertBefore(item, reference);\n      drake.emit('shadow', item, dropTarget, _source);\n    }\n    function moved (type) { drake.emit(type, item, _lastDropTarget, _source); }\n    function over () { if (changed) { moved('over'); } }\n    function out () { if (_lastDropTarget) { moved('out'); } }\n  }\n\n  function spillOver (el) {\n    classes.rm(el, 'gu-hide');\n  }\n\n  function spillOut (el) {\n    if (drake.dragging) { classes.add(el, 'gu-hide'); }\n  }\n\n  function renderMirrorImage () {\n    if (_mirror) {\n      return;\n    }\n    var rect = _item.getBoundingClientRect();\n    _mirror = _item.cloneNode(true);\n    _mirror.style.width = getRectWidth(rect) + 'px';\n    _mirror.style.height = getRectHeight(rect) + 'px';\n    classes.rm(_mirror, 'gu-transit');\n    classes.add(_mirror, 'gu-mirror');\n    o.mirrorContainer.appendChild(_mirror);\n    touchy(documentElement, 'add', 'mousemove', drag);\n    classes.add(o.mirrorContainer, 'gu-unselectable');\n    drake.emit('cloned', _mirror, _item, 'mirror');\n  }\n\n  function removeMirrorImage () {\n    if (_mirror) {\n      classes.rm(o.mirrorContainer, 'gu-unselectable');\n      touchy(documentElement, 'remove', 'mousemove', drag);\n      getParent(_mirror).removeChild(_mirror);\n      _mirror = null;\n    }\n  }\n\n  function getImmediateChild (dropTarget, target) {\n    var immediate = target;\n    while (immediate !== dropTarget && getParent(immediate) !== dropTarget) {\n      immediate = getParent(immediate);\n    }\n    if (immediate === documentElement) {\n      return null;\n    }\n    return immediate;\n  }\n\n  function getReference (dropTarget, target, x, y) {\n    var horizontal = o.direction === 'horizontal';\n    var reference = target !== dropTarget ? inside() : outside();\n    return reference;\n\n    function outside () { // slower, but able to figure out any position\n      var len = dropTarget.children.length;\n      var i;\n      var el;\n      var rect;\n      for (i = 0; i < len; i++) {\n        el = dropTarget.children[i];\n        rect = el.getBoundingClientRect();\n        if (horizontal && (rect.left + rect.width / 2) > x) { return el; }\n        if (!horizontal && (rect.top + rect.height / 2) > y) { return el; }\n      }\n      return null;\n    }\n\n    function inside () { // faster, but only available if dropped inside a child element\n      var rect = target.getBoundingClientRect();\n      if (horizontal) {\n        return resolve(x > rect.left + getRectWidth(rect) / 2);\n      }\n      return resolve(y > rect.top + getRectHeight(rect) / 2);\n    }\n\n    function resolve (after) {\n      return after ? nextEl(target) : target;\n    }\n  }\n\n  function isCopy (item, container) {\n    return typeof o.copy === 'boolean' ? o.copy : o.copy(item, container);\n  }\n}\n\nfunction touchy (el, op, type, fn) {\n  var touch = {\n    mouseup: 'touchend',\n    mousedown: 'touchstart',\n    mousemove: 'touchmove'\n  };\n  var pointers = {\n    mouseup: 'pointerup',\n    mousedown: 'pointerdown',\n    mousemove: 'pointermove'\n  };\n  var microsoft = {\n    mouseup: 'MSPointerUp',\n    mousedown: 'MSPointerDown',\n    mousemove: 'MSPointerMove'\n  };\n  if (global.navigator.pointerEnabled) {\n    crossvent[op](el, pointers[type], fn);\n  } else if (global.navigator.msPointerEnabled) {\n    crossvent[op](el, microsoft[type], fn);\n  } else {\n    crossvent[op](el, touch[type], fn);\n    crossvent[op](el, type, fn);\n  }\n}\n\nfunction whichMouseButton (e) {\n  if (e.touches !== void 0) { return e.touches.length; }\n  if (e.which !== void 0 && e.which !== 0) { return e.which; } // see https://github.com/bevacqua/dragula/issues/261\n  if (e.buttons !== void 0) { return e.buttons; }\n  var button = e.button;\n  if (button !== void 0) { // see https://github.com/jquery/jquery/blob/99e8ff1baa7ae341e94bb89c3e84570c7c3ad9ea/src/event.js#L573-L575\n    return button & 1 ? 1 : button & 2 ? 3 : (button & 4 ? 2 : 0);\n  }\n}\n\nfunction getOffset (el) {\n  var rect = el.getBoundingClientRect();\n  return {\n    left: rect.left + getScroll('scrollLeft', 'pageXOffset'),\n    top: rect.top + getScroll('scrollTop', 'pageYOffset')\n  };\n}\n\nfunction getScroll (scrollProp, offsetProp) {\n  if (typeof global[offsetProp] !== 'undefined') {\n    return global[offsetProp];\n  }\n  if (documentElement.clientHeight) {\n    return documentElement[scrollProp];\n  }\n  return doc.body[scrollProp];\n}\n\nfunction getElementBehindPoint (point, x, y) {\n  var p = point || {};\n  var state = p.className;\n  var el;\n  p.className += ' gu-hide';\n  el = doc.elementFromPoint(x, y);\n  p.className = state;\n  return el;\n}\n\nfunction never () { return false; }\nfunction always () { return true; }\nfunction getRectWidth (rect) { return rect.width || (rect.right - rect.left); }\nfunction getRectHeight (rect) { return rect.height || (rect.bottom - rect.top); }\nfunction getParent (el) { return el.parentNode === doc ? null : el.parentNode; }\nfunction isInput (el) { return el.tagName === 'INPUT' || el.tagName === 'TEXTAREA' || el.tagName === 'SELECT' || isEditable(el); }\nfunction isEditable (el) {\n  if (!el) { return false; } // no parents were editable\n  if (el.contentEditable === 'false') { return false; } // stop the lookup\n  if (el.contentEditable === 'true') { return true; } // found a contentEditable element in the chain\n  return isEditable(getParent(el)); // contentEditable is set to 'inherit'\n}\n\nfunction nextEl (el) {\n  return el.nextElementSibling || manually();\n  function manually () {\n    var sibling = el;\n    do {\n      sibling = sibling.nextSibling;\n    } while (sibling && sibling.nodeType !== 1);\n    return sibling;\n  }\n}\n\nfunction getEventHost (e) {\n  // on touchend event, we have to use `e.changedTouches`\n  // see http://stackoverflow.com/questions/7192563/touchend-event-properties\n  // see https://github.com/bevacqua/dragula/issues/34\n  if (e.targetTouches && e.targetTouches.length) {\n    return e.targetTouches[0];\n  }\n  if (e.changedTouches && e.changedTouches.length) {\n    return e.changedTouches[0];\n  }\n  return e;\n}\n\nfunction getCoord (coord, e) {\n  var host = getEventHost(e);\n  var missMap = {\n    pageX: 'clientX', // IE8\n    pageY: 'clientY' // IE8\n  };\n  if (coord in missMap && !(coord in host) && missMap[coord] in host) {\n    coord = missMap[coord];\n  }\n  return host[coord];\n}\n\nmodule.exports = dragula;\n\n/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../webpack/buildin/global.js */ \"./node_modules/webpack/buildin/global.js\")))\n\n//# sourceURL=webpack:///./node_modules/dragula/dragula.js?")},"./node_modules/evaluate-string/index.js":function(module,exports){eval("// const string = evaluateString('string')\n// const number = evaluateString('5')\n// const booleanFalse = evaluateString('false')\n// const booleanTrue = evaluateString('true')\n// const array = evaluateString('[1, 2]')\n// const object = evaluateString(`{id: 1, name: 'snow'}`)\n// const json = evaluateString('{\"id\": 1, \"name\": \"snow\"}')\n\nmodule.exports = evaluateString\n\nfunction evaluateString(value) {\n  try {\n    value = value.trim()\n    const isVariable = !value.startsWith('[')\n      && !value.startsWith('{')\n      && !value.startsWith('\\'')\n      && !value.startsWith('\"')\n      && !value.startsWith('`')\n      && value !== 'true'\n      && value !== 'false'\n      && isNaN(value)\n\n    return isVariable\n        ? eval(`'${value}'`) // convert to string\n        : eval(`(${value})`) // evaluate\n  } catch (e) {\n    return value\n  }\n}\n\n//# sourceURL=webpack:///./node_modules/evaluate-string/index.js?")},"./node_modules/fbjs/lib/emptyFunction.js":function(module,exports,__webpack_require__){"use strict";eval("\n\n/**\n * Copyright (c) 2013-present, Facebook, Inc.\n *\n * This source code is licensed under the MIT license found in the\n * LICENSE file in the root directory of this source tree.\n *\n * \n */\n\nfunction makeEmptyFunction(arg) {\n  return function () {\n    return arg;\n  };\n}\n\n/**\n * This function accepts and discards inputs; it has no side effects. This is\n * primarily useful idiomatically for overridable function endpoints which\n * always need to be callable, since JS lacks a null-call idiom ala Cocoa.\n */\nvar emptyFunction = function emptyFunction() {};\n\nemptyFunction.thatReturns = makeEmptyFunction;\nemptyFunction.thatReturnsFalse = makeEmptyFunction(false);\nemptyFunction.thatReturnsTrue = makeEmptyFunction(true);\nemptyFunction.thatReturnsNull = makeEmptyFunction(null);\nemptyFunction.thatReturnsThis = function () {\n  return this;\n};\nemptyFunction.thatReturnsArgument = function (arg) {\n  return arg;\n};\n\nmodule.exports = emptyFunction;\n\n//# sourceURL=webpack:///./node_modules/fbjs/lib/emptyFunction.js?")},"./node_modules/fbjs/lib/emptyObject.js":function(module,exports,__webpack_require__){"use strict";eval("/**\n * Copyright (c) 2013-present, Facebook, Inc.\n *\n * This source code is licensed under the MIT license found in the\n * LICENSE file in the root directory of this source tree.\n *\n */\n\n\n\nvar emptyObject = {};\n\nif (true) {\n  Object.freeze(emptyObject);\n}\n\nmodule.exports = emptyObject;\n\n//# sourceURL=webpack:///./node_modules/fbjs/lib/emptyObject.js?")},"./node_modules/fbjs/lib/invariant.js":function(module,exports,__webpack_require__){"use strict";eval("/**\n * Copyright (c) 2013-present, Facebook, Inc.\n *\n * This source code is licensed under the MIT license found in the\n * LICENSE file in the root directory of this source tree.\n *\n */\n\n\n\n/**\n * Use invariant() to assert state which your program assumes to be true.\n *\n * Provide sprintf-style format (only %s is supported) and arguments\n * to provide information about what broke and what you were\n * expecting.\n *\n * The invariant message will be stripped in production, but the invariant\n * will remain to ensure logic does not differ in production.\n */\n\nvar validateFormat = function validateFormat(format) {};\n\nif (true) {\n  validateFormat = function validateFormat(format) {\n    if (format === undefined) {\n      throw new Error('invariant requires an error message argument');\n    }\n  };\n}\n\nfunction invariant(condition, format, a, b, c, d, e, f) {\n  validateFormat(format);\n\n  if (!condition) {\n    var error;\n    if (format === undefined) {\n      error = new Error('Minified exception occurred; use the non-minified dev environment ' + 'for the full error message and additional helpful warnings.');\n    } else {\n      var args = [a, b, c, d, e, f];\n      var argIndex = 0;\n      error = new Error(format.replace(/%s/g, function () {\n        return args[argIndex++];\n      }));\n      error.name = 'Invariant Violation';\n    }\n\n    error.framesToPop = 1; // we don't care about invariant's own frame\n    throw error;\n  }\n}\n\nmodule.exports = invariant;\n\n//# sourceURL=webpack:///./node_modules/fbjs/lib/invariant.js?")},"./node_modules/fbjs/lib/warning.js":function(module,exports,__webpack_require__){"use strict";eval("/**\n * Copyright (c) 2014-present, Facebook, Inc.\n *\n * This source code is licensed under the MIT license found in the\n * LICENSE file in the root directory of this source tree.\n *\n */\n\n\n\nvar emptyFunction = __webpack_require__(/*! ./emptyFunction */ \"./node_modules/fbjs/lib/emptyFunction.js\");\n\n/**\n * Similar to invariant but only logs a warning if the condition is not met.\n * This can be used to log issues in development environments in critical\n * paths. Removing the logging code for production environments will keep the\n * same logic and follow the same code paths.\n */\n\nvar warning = emptyFunction;\n\nif (true) {\n  var printWarning = function printWarning(format) {\n    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {\n      args[_key - 1] = arguments[_key];\n    }\n\n    var argIndex = 0;\n    var message = 'Warning: ' + format.replace(/%s/g, function () {\n      return args[argIndex++];\n    });\n    if (typeof console !== 'undefined') {\n      console.error(message);\n    }\n    try {\n      // --- Welcome to debugging React ---\n      // This error was thrown as a convenience so that you can use this stack\n      // to find the callsite that caused this warning to fire.\n      throw new Error(message);\n    } catch (x) {}\n  };\n\n  warning = function warning(condition, format) {\n    if (format === undefined) {\n      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');\n    }\n\n    if (format.indexOf('Failed Composite propType: ') === 0) {\n      return; // Ignore CompositeComponent proptype check.\n    }\n\n    if (!condition) {\n      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {\n        args[_key2 - 2] = arguments[_key2];\n      }\n\n      printWarning.apply(undefined, [format].concat(args));\n    }\n  };\n}\n\nmodule.exports = warning;\n\n//# sourceURL=webpack:///./node_modules/fbjs/lib/warning.js?")},"./node_modules/object-assign/index.js":function(module,exports,__webpack_require__){"use strict";eval("/*\nobject-assign\n(c) Sindre Sorhus\n@license MIT\n*/\n\n\n/* eslint-disable no-unused-vars */\nvar getOwnPropertySymbols = Object.getOwnPropertySymbols;\nvar hasOwnProperty = Object.prototype.hasOwnProperty;\nvar propIsEnumerable = Object.prototype.propertyIsEnumerable;\n\nfunction toObject(val) {\n\tif (val === null || val === undefined) {\n\t\tthrow new TypeError('Object.assign cannot be called with null or undefined');\n\t}\n\n\treturn Object(val);\n}\n\nfunction shouldUseNative() {\n\ttry {\n\t\tif (!Object.assign) {\n\t\t\treturn false;\n\t\t}\n\n\t\t// Detect buggy property enumeration order in older V8 versions.\n\n\t\t// https://bugs.chromium.org/p/v8/issues/detail?id=4118\n\t\tvar test1 = new String('abc');  // eslint-disable-line no-new-wrappers\n\t\ttest1[5] = 'de';\n\t\tif (Object.getOwnPropertyNames(test1)[0] === '5') {\n\t\t\treturn false;\n\t\t}\n\n\t\t// https://bugs.chromium.org/p/v8/issues/detail?id=3056\n\t\tvar test2 = {};\n\t\tfor (var i = 0; i < 10; i++) {\n\t\t\ttest2['_' + String.fromCharCode(i)] = i;\n\t\t}\n\t\tvar order2 = Object.getOwnPropertyNames(test2).map(function (n) {\n\t\t\treturn test2[n];\n\t\t});\n\t\tif (order2.join('') !== '0123456789') {\n\t\t\treturn false;\n\t\t}\n\n\t\t// https://bugs.chromium.org/p/v8/issues/detail?id=3056\n\t\tvar test3 = {};\n\t\t'abcdefghijklmnopqrst'.split('').forEach(function (letter) {\n\t\t\ttest3[letter] = letter;\n\t\t});\n\t\tif (Object.keys(Object.assign({}, test3)).join('') !==\n\t\t\t\t'abcdefghijklmnopqrst') {\n\t\t\treturn false;\n\t\t}\n\n\t\treturn true;\n\t} catch (err) {\n\t\t// We don't expect any of the above to throw, but better to be safe.\n\t\treturn false;\n\t}\n}\n\nmodule.exports = shouldUseNative() ? Object.assign : function (target, source) {\n\tvar from;\n\tvar to = toObject(target);\n\tvar symbols;\n\n\tfor (var s = 1; s < arguments.length; s++) {\n\t\tfrom = Object(arguments[s]);\n\n\t\tfor (var key in from) {\n\t\t\tif (hasOwnProperty.call(from, key)) {\n\t\t\t\tto[key] = from[key];\n\t\t\t}\n\t\t}\n\n\t\tif (getOwnPropertySymbols) {\n\t\t\tsymbols = getOwnPropertySymbols(from);\n\t\t\tfor (var i = 0; i < symbols.length; i++) {\n\t\t\t\tif (propIsEnumerable.call(from, symbols[i])) {\n\t\t\t\t\tto[symbols[i]] = from[symbols[i]];\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\t}\n\n\treturn to;\n};\n\n\n//# sourceURL=webpack:///./node_modules/object-assign/index.js?")},"./node_modules/process/browser.js":function(module,exports){eval("// shim for using process in browser\nvar process = module.exports = {};\n\n// cached from whatever global is present so that test runners that stub it\n// don't break things.  But we need to wrap it in a try catch in case it is\n// wrapped in strict mode code which doesn't define any globals.  It's inside a\n// function because try/catches deoptimize in certain engines.\n\nvar cachedSetTimeout;\nvar cachedClearTimeout;\n\nfunction defaultSetTimout() {\n    throw new Error('setTimeout has not been defined');\n}\nfunction defaultClearTimeout () {\n    throw new Error('clearTimeout has not been defined');\n}\n(function () {\n    try {\n        if (typeof setTimeout === 'function') {\n            cachedSetTimeout = setTimeout;\n        } else {\n            cachedSetTimeout = defaultSetTimout;\n        }\n    } catch (e) {\n        cachedSetTimeout = defaultSetTimout;\n    }\n    try {\n        if (typeof clearTimeout === 'function') {\n            cachedClearTimeout = clearTimeout;\n        } else {\n            cachedClearTimeout = defaultClearTimeout;\n        }\n    } catch (e) {\n        cachedClearTimeout = defaultClearTimeout;\n    }\n} ())\nfunction runTimeout(fun) {\n    if (cachedSetTimeout === setTimeout) {\n        //normal enviroments in sane situations\n        return setTimeout(fun, 0);\n    }\n    // if setTimeout wasn't available but was latter defined\n    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {\n        cachedSetTimeout = setTimeout;\n        return setTimeout(fun, 0);\n    }\n    try {\n        // when when somebody has screwed with setTimeout but no I.E. maddness\n        return cachedSetTimeout(fun, 0);\n    } catch(e){\n        try {\n            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally\n            return cachedSetTimeout.call(null, fun, 0);\n        } catch(e){\n            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error\n            return cachedSetTimeout.call(this, fun, 0);\n        }\n    }\n\n\n}\nfunction runClearTimeout(marker) {\n    if (cachedClearTimeout === clearTimeout) {\n        //normal enviroments in sane situations\n        return clearTimeout(marker);\n    }\n    // if clearTimeout wasn't available but was latter defined\n    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {\n        cachedClearTimeout = clearTimeout;\n        return clearTimeout(marker);\n    }\n    try {\n        // when when somebody has screwed with setTimeout but no I.E. maddness\n        return cachedClearTimeout(marker);\n    } catch (e){\n        try {\n            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally\n            return cachedClearTimeout.call(null, marker);\n        } catch (e){\n            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.\n            // Some versions of I.E. have different rules for clearTimeout vs setTimeout\n            return cachedClearTimeout.call(this, marker);\n        }\n    }\n\n\n\n}\nvar queue = [];\nvar draining = false;\nvar currentQueue;\nvar queueIndex = -1;\n\nfunction cleanUpNextTick() {\n    if (!draining || !currentQueue) {\n        return;\n    }\n    draining = false;\n    if (currentQueue.length) {\n        queue = currentQueue.concat(queue);\n    } else {\n        queueIndex = -1;\n    }\n    if (queue.length) {\n        drainQueue();\n    }\n}\n\nfunction drainQueue() {\n    if (draining) {\n        return;\n    }\n    var timeout = runTimeout(cleanUpNextTick);\n    draining = true;\n\n    var len = queue.length;\n    while(len) {\n        currentQueue = queue;\n        queue = [];\n        while (++queueIndex < len) {\n            if (currentQueue) {\n                currentQueue[queueIndex].run();\n            }\n        }\n        queueIndex = -1;\n        len = queue.length;\n    }\n    currentQueue = null;\n    draining = false;\n    runClearTimeout(timeout);\n}\n\nprocess.nextTick = function (fun) {\n    var args = new Array(arguments.length - 1);\n    if (arguments.length > 1) {\n        for (var i = 1; i < arguments.length; i++) {\n            args[i - 1] = arguments[i];\n        }\n    }\n    queue.push(new Item(fun, args));\n    if (queue.length === 1 && !draining) {\n        runTimeout(drainQueue);\n    }\n};\n\n// v8 likes predictible objects\nfunction Item(fun, array) {\n    this.fun = fun;\n    this.array = array;\n}\nItem.prototype.run = function () {\n    this.fun.apply(null, this.array);\n};\nprocess.title = 'browser';\nprocess.browser = true;\nprocess.env = {};\nprocess.argv = [];\nprocess.version = ''; // empty string to avoid regexp issues\nprocess.versions = {};\n\nfunction noop() {}\n\nprocess.on = noop;\nprocess.addListener = noop;\nprocess.once = noop;\nprocess.off = noop;\nprocess.removeListener = noop;\nprocess.removeAllListeners = noop;\nprocess.emit = noop;\nprocess.prependListener = noop;\nprocess.prependOnceListener = noop;\n\nprocess.listeners = function (name) { return [] }\n\nprocess.binding = function (name) {\n    throw new Error('process.binding is not supported');\n};\n\nprocess.cwd = function () { return '/' };\nprocess.chdir = function (dir) {\n    throw new Error('process.chdir is not supported');\n};\nprocess.umask = function() { return 0; };\n\n\n//# sourceURL=webpack:///./node_modules/process/browser.js?")},"./node_modules/prop-types/checkPropTypes.js":function(module,exports,__webpack_require__){"use strict";eval("/**\n * Copyright (c) 2013-present, Facebook, Inc.\n *\n * This source code is licensed under the MIT license found in the\n * LICENSE file in the root directory of this source tree.\n */\n\n\n\nif (true) {\n  var invariant = __webpack_require__(/*! fbjs/lib/invariant */ \"./node_modules/fbjs/lib/invariant.js\");\n  var warning = __webpack_require__(/*! fbjs/lib/warning */ \"./node_modules/fbjs/lib/warning.js\");\n  var ReactPropTypesSecret = __webpack_require__(/*! ./lib/ReactPropTypesSecret */ \"./node_modules/prop-types/lib/ReactPropTypesSecret.js\");\n  var loggedTypeFailures = {};\n}\n\n/**\n * Assert that the values match with the type specs.\n * Error messages are memorized and will only be shown once.\n *\n * @param {object} typeSpecs Map of name to a ReactPropType\n * @param {object} values Runtime values that need to be type-checked\n * @param {string} location e.g. \"prop\", \"context\", \"child context\"\n * @param {string} componentName Name of the component for error messages.\n * @param {?Function} getStack Returns the component stack.\n * @private\n */\nfunction checkPropTypes(typeSpecs, values, location, componentName, getStack) {\n  if (true) {\n    for (var typeSpecName in typeSpecs) {\n      if (typeSpecs.hasOwnProperty(typeSpecName)) {\n        var error;\n        // Prop type validation may throw. In case they do, we don't want to\n        // fail the render phase where it didn't fail before. So we log it.\n        // After these have been cleaned up, we'll let them throw.\n        try {\n          // This is intentionally an invariant that gets caught. It's the same\n          // behavior as without this statement except with a better message.\n          invariant(typeof typeSpecs[typeSpecName] === 'function', '%s: %s type `%s` is invalid; it must be a function, usually from ' + 'the `prop-types` package, but received `%s`.', componentName || 'React class', location, typeSpecName, typeof typeSpecs[typeSpecName]);\n          error = typeSpecs[typeSpecName](values, typeSpecName, componentName, location, null, ReactPropTypesSecret);\n        } catch (ex) {\n          error = ex;\n        }\n        warning(!error || error instanceof Error, '%s: type specification of %s `%s` is invalid; the type checker ' + 'function must return `null` or an `Error` but returned a %s. ' + 'You may have forgotten to pass an argument to the type checker ' + 'creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and ' + 'shape all require an argument).', componentName || 'React class', location, typeSpecName, typeof error);\n        if (error instanceof Error && !(error.message in loggedTypeFailures)) {\n          // Only monitor this failure once because there tends to be a lot of the\n          // same error.\n          loggedTypeFailures[error.message] = true;\n\n          var stack = getStack ? getStack() : '';\n\n          warning(false, 'Failed %s type: %s%s', location, error.message, stack != null ? stack : '');\n        }\n      }\n    }\n  }\n}\n\nmodule.exports = checkPropTypes;\n\n\n//# sourceURL=webpack:///./node_modules/prop-types/checkPropTypes.js?")},"./node_modules/prop-types/lib/ReactPropTypesSecret.js":function(module,exports,__webpack_require__){"use strict";eval("/**\n * Copyright (c) 2013-present, Facebook, Inc.\n *\n * This source code is licensed under the MIT license found in the\n * LICENSE file in the root directory of this source tree.\n */\n\n\n\nvar ReactPropTypesSecret = 'SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED';\n\nmodule.exports = ReactPropTypesSecret;\n\n\n//# sourceURL=webpack:///./node_modules/prop-types/lib/ReactPropTypesSecret.js?")},"./node_modules/react/cjs/react.development.js":function(module,exports,__webpack_require__){"use strict";eval("/** @license React v16.2.0\n * react.development.js\n *\n * Copyright (c) 2013-present, Facebook, Inc.\n *\n * This source code is licensed under the MIT license found in the\n * LICENSE file in the root directory of this source tree.\n */\n\n\n\n\n\nif (true) {\n  (function() {\n'use strict';\n\nvar _assign = __webpack_require__(/*! object-assign */ \"./node_modules/object-assign/index.js\");\nvar emptyObject = __webpack_require__(/*! fbjs/lib/emptyObject */ \"./node_modules/fbjs/lib/emptyObject.js\");\nvar invariant = __webpack_require__(/*! fbjs/lib/invariant */ \"./node_modules/fbjs/lib/invariant.js\");\nvar warning = __webpack_require__(/*! fbjs/lib/warning */ \"./node_modules/fbjs/lib/warning.js\");\nvar emptyFunction = __webpack_require__(/*! fbjs/lib/emptyFunction */ \"./node_modules/fbjs/lib/emptyFunction.js\");\nvar checkPropTypes = __webpack_require__(/*! prop-types/checkPropTypes */ \"./node_modules/prop-types/checkPropTypes.js\");\n\n// TODO: this is special because it gets imported during build.\n\nvar ReactVersion = '16.2.0';\n\n// The Symbol used to tag the ReactElement-like types. If there is no native Symbol\n// nor polyfill, then a plain number is used for performance.\nvar hasSymbol = typeof Symbol === 'function' && Symbol['for'];\n\nvar REACT_ELEMENT_TYPE = hasSymbol ? Symbol['for']('react.element') : 0xeac7;\nvar REACT_CALL_TYPE = hasSymbol ? Symbol['for']('react.call') : 0xeac8;\nvar REACT_RETURN_TYPE = hasSymbol ? Symbol['for']('react.return') : 0xeac9;\nvar REACT_PORTAL_TYPE = hasSymbol ? Symbol['for']('react.portal') : 0xeaca;\nvar REACT_FRAGMENT_TYPE = hasSymbol ? Symbol['for']('react.fragment') : 0xeacb;\n\nvar MAYBE_ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;\nvar FAUX_ITERATOR_SYMBOL = '@@iterator';\n\nfunction getIteratorFn(maybeIterable) {\n  if (maybeIterable === null || typeof maybeIterable === 'undefined') {\n    return null;\n  }\n  var maybeIterator = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL];\n  if (typeof maybeIterator === 'function') {\n    return maybeIterator;\n  }\n  return null;\n}\n\n/**\n * WARNING: DO NOT manually require this module.\n * This is a replacement for `invariant(...)` used by the error code system\n * and will _only_ be required by the corresponding babel pass.\n * It always throws.\n */\n\n/**\n * Forked from fbjs/warning:\n * https://github.com/facebook/fbjs/blob/e66ba20ad5be433eb54423f2b097d829324d9de6/packages/fbjs/src/__forks__/warning.js\n *\n * Only change is we use console.warn instead of console.error,\n * and do nothing when 'console' is not supported.\n * This really simplifies the code.\n * ---\n * Similar to invariant but only logs a warning if the condition is not met.\n * This can be used to log issues in development environments in critical\n * paths. Removing the logging code for production environments will keep the\n * same logic and follow the same code paths.\n */\n\nvar lowPriorityWarning = function () {};\n\n{\n  var printWarning = function (format) {\n    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {\n      args[_key - 1] = arguments[_key];\n    }\n\n    var argIndex = 0;\n    var message = 'Warning: ' + format.replace(/%s/g, function () {\n      return args[argIndex++];\n    });\n    if (typeof console !== 'undefined') {\n      console.warn(message);\n    }\n    try {\n      // --- Welcome to debugging React ---\n      // This error was thrown as a convenience so that you can use this stack\n      // to find the callsite that caused this warning to fire.\n      throw new Error(message);\n    } catch (x) {}\n  };\n\n  lowPriorityWarning = function (condition, format) {\n    if (format === undefined) {\n      throw new Error('`warning(condition, format, ...args)` requires a warning ' + 'message argument');\n    }\n    if (!condition) {\n      for (var _len2 = arguments.length, args = Array(_len2 > 2 ? _len2 - 2 : 0), _key2 = 2; _key2 < _len2; _key2++) {\n        args[_key2 - 2] = arguments[_key2];\n      }\n\n      printWarning.apply(undefined, [format].concat(args));\n    }\n  };\n}\n\nvar lowPriorityWarning$1 = lowPriorityWarning;\n\nvar didWarnStateUpdateForUnmountedComponent = {};\n\nfunction warnNoop(publicInstance, callerName) {\n  {\n    var constructor = publicInstance.constructor;\n    var componentName = constructor && (constructor.displayName || constructor.name) || 'ReactClass';\n    var warningKey = componentName + '.' + callerName;\n    if (didWarnStateUpdateForUnmountedComponent[warningKey]) {\n      return;\n    }\n    warning(false, '%s(...): Can only update a mounted or mounting component. ' + 'This usually means you called %s() on an unmounted component. ' + 'This is a no-op.\\n\\nPlease check the code for the %s component.', callerName, callerName, componentName);\n    didWarnStateUpdateForUnmountedComponent[warningKey] = true;\n  }\n}\n\n/**\n * This is the abstract API for an update queue.\n */\nvar ReactNoopUpdateQueue = {\n  /**\n   * Checks whether or not this composite component is mounted.\n   * @param {ReactClass} publicInstance The instance we want to test.\n   * @return {boolean} True if mounted, false otherwise.\n   * @protected\n   * @final\n   */\n  isMounted: function (publicInstance) {\n    return false;\n  },\n\n  /**\n   * Forces an update. This should only be invoked when it is known with\n   * certainty that we are **not** in a DOM transaction.\n   *\n   * You may want to call this when you know that some deeper aspect of the\n   * component's state has changed but `setState` was not called.\n   *\n   * This will not invoke `shouldComponentUpdate`, but it will invoke\n   * `componentWillUpdate` and `componentDidUpdate`.\n   *\n   * @param {ReactClass} publicInstance The instance that should rerender.\n   * @param {?function} callback Called after component is updated.\n   * @param {?string} callerName name of the calling function in the public API.\n   * @internal\n   */\n  enqueueForceUpdate: function (publicInstance, callback, callerName) {\n    warnNoop(publicInstance, 'forceUpdate');\n  },\n\n  /**\n   * Replaces all of the state. Always use this or `setState` to mutate state.\n   * You should treat `this.state` as immutable.\n   *\n   * There is no guarantee that `this.state` will be immediately updated, so\n   * accessing `this.state` after calling this method may return the old value.\n   *\n   * @param {ReactClass} publicInstance The instance that should rerender.\n   * @param {object} completeState Next state.\n   * @param {?function} callback Called after component is updated.\n   * @param {?string} callerName name of the calling function in the public API.\n   * @internal\n   */\n  enqueueReplaceState: function (publicInstance, completeState, callback, callerName) {\n    warnNoop(publicInstance, 'replaceState');\n  },\n\n  /**\n   * Sets a subset of the state. This only exists because _pendingState is\n   * internal. This provides a merging strategy that is not available to deep\n   * properties which is confusing. TODO: Expose pendingState or don't use it\n   * during the merge.\n   *\n   * @param {ReactClass} publicInstance The instance that should rerender.\n   * @param {object} partialState Next partial state to be merged with state.\n   * @param {?function} callback Called after component is updated.\n   * @param {?string} Name of the calling function in the public API.\n   * @internal\n   */\n  enqueueSetState: function (publicInstance, partialState, callback, callerName) {\n    warnNoop(publicInstance, 'setState');\n  }\n};\n\n/**\n * Base class helpers for the updating state of a component.\n */\nfunction Component(props, context, updater) {\n  this.props = props;\n  this.context = context;\n  this.refs = emptyObject;\n  // We initialize the default updater but the real one gets injected by the\n  // renderer.\n  this.updater = updater || ReactNoopUpdateQueue;\n}\n\nComponent.prototype.isReactComponent = {};\n\n/**\n * Sets a subset of the state. Always use this to mutate\n * state. You should treat `this.state` as immutable.\n *\n * There is no guarantee that `this.state` will be immediately updated, so\n * accessing `this.state` after calling this method may return the old value.\n *\n * There is no guarantee that calls to `setState` will run synchronously,\n * as they may eventually be batched together.  You can provide an optional\n * callback that will be executed when the call to setState is actually\n * completed.\n *\n * When a function is provided to setState, it will be called at some point in\n * the future (not synchronously). It will be called with the up to date\n * component arguments (state, props, context). These values can be different\n * from this.* because your function may be called after receiveProps but before\n * shouldComponentUpdate, and this new state, props, and context will not yet be\n * assigned to this.\n *\n * @param {object|function} partialState Next partial state or function to\n *        produce next partial state to be merged with current state.\n * @param {?function} callback Called after state is updated.\n * @final\n * @protected\n */\nComponent.prototype.setState = function (partialState, callback) {\n  !(typeof partialState === 'object' || typeof partialState === 'function' || partialState == null) ? invariant(false, 'setState(...): takes an object of state variables to update or a function which returns an object of state variables.') : void 0;\n  this.updater.enqueueSetState(this, partialState, callback, 'setState');\n};\n\n/**\n * Forces an update. This should only be invoked when it is known with\n * certainty that we are **not** in a DOM transaction.\n *\n * You may want to call this when you know that some deeper aspect of the\n * component's state has changed but `setState` was not called.\n *\n * This will not invoke `shouldComponentUpdate`, but it will invoke\n * `componentWillUpdate` and `componentDidUpdate`.\n *\n * @param {?function} callback Called after update is complete.\n * @final\n * @protected\n */\nComponent.prototype.forceUpdate = function (callback) {\n  this.updater.enqueueForceUpdate(this, callback, 'forceUpdate');\n};\n\n/**\n * Deprecated APIs. These APIs used to exist on classic React classes but since\n * we would like to deprecate them, we're not going to move them over to this\n * modern base class. Instead, we define a getter that warns if it's accessed.\n */\n{\n  var deprecatedAPIs = {\n    isMounted: ['isMounted', 'Instead, make sure to clean up subscriptions and pending requests in ' + 'componentWillUnmount to prevent memory leaks.'],\n    replaceState: ['replaceState', 'Refactor your code to use setState instead (see ' + 'https://github.com/facebook/react/issues/3236).']\n  };\n  var defineDeprecationWarning = function (methodName, info) {\n    Object.defineProperty(Component.prototype, methodName, {\n      get: function () {\n        lowPriorityWarning$1(false, '%s(...) is deprecated in plain JavaScript React classes. %s', info[0], info[1]);\n        return undefined;\n      }\n    });\n  };\n  for (var fnName in deprecatedAPIs) {\n    if (deprecatedAPIs.hasOwnProperty(fnName)) {\n      defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);\n    }\n  }\n}\n\n/**\n * Base class helpers for the updating state of a component.\n */\nfunction PureComponent(props, context, updater) {\n  // Duplicated from Component.\n  this.props = props;\n  this.context = context;\n  this.refs = emptyObject;\n  // We initialize the default updater but the real one gets injected by the\n  // renderer.\n  this.updater = updater || ReactNoopUpdateQueue;\n}\n\nfunction ComponentDummy() {}\nComponentDummy.prototype = Component.prototype;\nvar pureComponentPrototype = PureComponent.prototype = new ComponentDummy();\npureComponentPrototype.constructor = PureComponent;\n// Avoid an extra prototype jump for these methods.\n_assign(pureComponentPrototype, Component.prototype);\npureComponentPrototype.isPureReactComponent = true;\n\nfunction AsyncComponent(props, context, updater) {\n  // Duplicated from Component.\n  this.props = props;\n  this.context = context;\n  this.refs = emptyObject;\n  // We initialize the default updater but the real one gets injected by the\n  // renderer.\n  this.updater = updater || ReactNoopUpdateQueue;\n}\n\nvar asyncComponentPrototype = AsyncComponent.prototype = new ComponentDummy();\nasyncComponentPrototype.constructor = AsyncComponent;\n// Avoid an extra prototype jump for these methods.\n_assign(asyncComponentPrototype, Component.prototype);\nasyncComponentPrototype.unstable_isAsyncReactComponent = true;\nasyncComponentPrototype.render = function () {\n  return this.props.children;\n};\n\n/**\n * Keeps track of the current owner.\n *\n * The current owner is the component who should own any components that are\n * currently being constructed.\n */\nvar ReactCurrentOwner = {\n  /**\n   * @internal\n   * @type {ReactComponent}\n   */\n  current: null\n};\n\nvar hasOwnProperty = Object.prototype.hasOwnProperty;\n\nvar RESERVED_PROPS = {\n  key: true,\n  ref: true,\n  __self: true,\n  __source: true\n};\n\nvar specialPropKeyWarningShown;\nvar specialPropRefWarningShown;\n\nfunction hasValidRef(config) {\n  {\n    if (hasOwnProperty.call(config, 'ref')) {\n      var getter = Object.getOwnPropertyDescriptor(config, 'ref').get;\n      if (getter && getter.isReactWarning) {\n        return false;\n      }\n    }\n  }\n  return config.ref !== undefined;\n}\n\nfunction hasValidKey(config) {\n  {\n    if (hasOwnProperty.call(config, 'key')) {\n      var getter = Object.getOwnPropertyDescriptor(config, 'key').get;\n      if (getter && getter.isReactWarning) {\n        return false;\n      }\n    }\n  }\n  return config.key !== undefined;\n}\n\nfunction defineKeyPropWarningGetter(props, displayName) {\n  var warnAboutAccessingKey = function () {\n    if (!specialPropKeyWarningShown) {\n      specialPropKeyWarningShown = true;\n      warning(false, '%s: `key` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);\n    }\n  };\n  warnAboutAccessingKey.isReactWarning = true;\n  Object.defineProperty(props, 'key', {\n    get: warnAboutAccessingKey,\n    configurable: true\n  });\n}\n\nfunction defineRefPropWarningGetter(props, displayName) {\n  var warnAboutAccessingRef = function () {\n    if (!specialPropRefWarningShown) {\n      specialPropRefWarningShown = true;\n      warning(false, '%s: `ref` is not a prop. Trying to access it will result ' + 'in `undefined` being returned. If you need to access the same ' + 'value within the child component, you should pass it as a different ' + 'prop. (https://fb.me/react-special-props)', displayName);\n    }\n  };\n  warnAboutAccessingRef.isReactWarning = true;\n  Object.defineProperty(props, 'ref', {\n    get: warnAboutAccessingRef,\n    configurable: true\n  });\n}\n\n/**\n * Factory method to create a new React element. This no longer adheres to\n * the class pattern, so do not use new to call it. Also, no instanceof check\n * will work. Instead test $$typeof field against Symbol.for('react.element') to check\n * if something is a React Element.\n *\n * @param {*} type\n * @param {*} key\n * @param {string|object} ref\n * @param {*} self A *temporary* helper to detect places where `this` is\n * different from the `owner` when React.createElement is called, so that we\n * can warn. We want to get rid of owner and replace string `ref`s with arrow\n * functions, and as long as `this` and owner are the same, there will be no\n * change in behavior.\n * @param {*} source An annotation object (added by a transpiler or otherwise)\n * indicating filename, line number, and/or other information.\n * @param {*} owner\n * @param {*} props\n * @internal\n */\nvar ReactElement = function (type, key, ref, self, source, owner, props) {\n  var element = {\n    // This tag allow us to uniquely identify this as a React Element\n    $$typeof: REACT_ELEMENT_TYPE,\n\n    // Built-in properties that belong on the element\n    type: type,\n    key: key,\n    ref: ref,\n    props: props,\n\n    // Record the component responsible for creating this element.\n    _owner: owner\n  };\n\n  {\n    // The validation flag is currently mutative. We put it on\n    // an external backing store so that we can freeze the whole object.\n    // This can be replaced with a WeakMap once they are implemented in\n    // commonly used development environments.\n    element._store = {};\n\n    // To make comparing ReactElements easier for testing purposes, we make\n    // the validation flag non-enumerable (where possible, which should\n    // include every environment we run tests in), so the test framework\n    // ignores it.\n    Object.defineProperty(element._store, 'validated', {\n      configurable: false,\n      enumerable: false,\n      writable: true,\n      value: false\n    });\n    // self and source are DEV only properties.\n    Object.defineProperty(element, '_self', {\n      configurable: false,\n      enumerable: false,\n      writable: false,\n      value: self\n    });\n    // Two elements created in two different places should be considered\n    // equal for testing purposes and therefore we hide it from enumeration.\n    Object.defineProperty(element, '_source', {\n      configurable: false,\n      enumerable: false,\n      writable: false,\n      value: source\n    });\n    if (Object.freeze) {\n      Object.freeze(element.props);\n      Object.freeze(element);\n    }\n  }\n\n  return element;\n};\n\n/**\n * Create and return a new ReactElement of the given type.\n * See https://reactjs.org/docs/react-api.html#createelement\n */\nfunction createElement(type, config, children) {\n  var propName;\n\n  // Reserved names are extracted\n  var props = {};\n\n  var key = null;\n  var ref = null;\n  var self = null;\n  var source = null;\n\n  if (config != null) {\n    if (hasValidRef(config)) {\n      ref = config.ref;\n    }\n    if (hasValidKey(config)) {\n      key = '' + config.key;\n    }\n\n    self = config.__self === undefined ? null : config.__self;\n    source = config.__source === undefined ? null : config.__source;\n    // Remaining properties are added to a new props object\n    for (propName in config) {\n      if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {\n        props[propName] = config[propName];\n      }\n    }\n  }\n\n  // Children can be more than one argument, and those are transferred onto\n  // the newly allocated props object.\n  var childrenLength = arguments.length - 2;\n  if (childrenLength === 1) {\n    props.children = children;\n  } else if (childrenLength > 1) {\n    var childArray = Array(childrenLength);\n    for (var i = 0; i < childrenLength; i++) {\n      childArray[i] = arguments[i + 2];\n    }\n    {\n      if (Object.freeze) {\n        Object.freeze(childArray);\n      }\n    }\n    props.children = childArray;\n  }\n\n  // Resolve default props\n  if (type && type.defaultProps) {\n    var defaultProps = type.defaultProps;\n    for (propName in defaultProps) {\n      if (props[propName] === undefined) {\n        props[propName] = defaultProps[propName];\n      }\n    }\n  }\n  {\n    if (key || ref) {\n      if (typeof props.$$typeof === 'undefined' || props.$$typeof !== REACT_ELEMENT_TYPE) {\n        var displayName = typeof type === 'function' ? type.displayName || type.name || 'Unknown' : type;\n        if (key) {\n          defineKeyPropWarningGetter(props, displayName);\n        }\n        if (ref) {\n          defineRefPropWarningGetter(props, displayName);\n        }\n      }\n    }\n  }\n  return ReactElement(type, key, ref, self, source, ReactCurrentOwner.current, props);\n}\n\n/**\n * Return a function that produces ReactElements of a given type.\n * See https://reactjs.org/docs/react-api.html#createfactory\n */\n\n\nfunction cloneAndReplaceKey(oldElement, newKey) {\n  var newElement = ReactElement(oldElement.type, newKey, oldElement.ref, oldElement._self, oldElement._source, oldElement._owner, oldElement.props);\n\n  return newElement;\n}\n\n/**\n * Clone and return a new ReactElement using element as the starting point.\n * See https://reactjs.org/docs/react-api.html#cloneelement\n */\nfunction cloneElement(element, config, children) {\n  var propName;\n\n  // Original props are copied\n  var props = _assign({}, element.props);\n\n  // Reserved names are extracted\n  var key = element.key;\n  var ref = element.ref;\n  // Self is preserved since the owner is preserved.\n  var self = element._self;\n  // Source is preserved since cloneElement is unlikely to be targeted by a\n  // transpiler, and the original source is probably a better indicator of the\n  // true owner.\n  var source = element._source;\n\n  // Owner will be preserved, unless ref is overridden\n  var owner = element._owner;\n\n  if (config != null) {\n    if (hasValidRef(config)) {\n      // Silently steal the ref from the parent.\n      ref = config.ref;\n      owner = ReactCurrentOwner.current;\n    }\n    if (hasValidKey(config)) {\n      key = '' + config.key;\n    }\n\n    // Remaining properties override existing props\n    var defaultProps;\n    if (element.type && element.type.defaultProps) {\n      defaultProps = element.type.defaultProps;\n    }\n    for (propName in config) {\n      if (hasOwnProperty.call(config, propName) && !RESERVED_PROPS.hasOwnProperty(propName)) {\n        if (config[propName] === undefined && defaultProps !== undefined) {\n          // Resolve default props\n          props[propName] = defaultProps[propName];\n        } else {\n          props[propName] = config[propName];\n        }\n      }\n    }\n  }\n\n  // Children can be more than one argument, and those are transferred onto\n  // the newly allocated props object.\n  var childrenLength = arguments.length - 2;\n  if (childrenLength === 1) {\n    props.children = children;\n  } else if (childrenLength > 1) {\n    var childArray = Array(childrenLength);\n    for (var i = 0; i < childrenLength; i++) {\n      childArray[i] = arguments[i + 2];\n    }\n    props.children = childArray;\n  }\n\n  return ReactElement(element.type, key, ref, self, source, owner, props);\n}\n\n/**\n * Verifies the object is a ReactElement.\n * See https://reactjs.org/docs/react-api.html#isvalidelement\n * @param {?object} object\n * @return {boolean} True if `object` is a valid component.\n * @final\n */\nfunction isValidElement(object) {\n  return typeof object === 'object' && object !== null && object.$$typeof === REACT_ELEMENT_TYPE;\n}\n\nvar ReactDebugCurrentFrame = {};\n\n{\n  // Component that is being worked on\n  ReactDebugCurrentFrame.getCurrentStack = null;\n\n  ReactDebugCurrentFrame.getStackAddendum = function () {\n    var impl = ReactDebugCurrentFrame.getCurrentStack;\n    if (impl) {\n      return impl();\n    }\n    return null;\n  };\n}\n\nvar SEPARATOR = '.';\nvar SUBSEPARATOR = ':';\n\n/**\n * Escape and wrap key so it is safe to use as a reactid\n *\n * @param {string} key to be escaped.\n * @return {string} the escaped key.\n */\nfunction escape(key) {\n  var escapeRegex = /[=:]/g;\n  var escaperLookup = {\n    '=': '=0',\n    ':': '=2'\n  };\n  var escapedString = ('' + key).replace(escapeRegex, function (match) {\n    return escaperLookup[match];\n  });\n\n  return '$' + escapedString;\n}\n\n/**\n * TODO: Test that a single child and an array with one item have the same key\n * pattern.\n */\n\nvar didWarnAboutMaps = false;\n\nvar userProvidedKeyEscapeRegex = /\\/+/g;\nfunction escapeUserProvidedKey(text) {\n  return ('' + text).replace(userProvidedKeyEscapeRegex, '$&/');\n}\n\nvar POOL_SIZE = 10;\nvar traverseContextPool = [];\nfunction getPooledTraverseContext(mapResult, keyPrefix, mapFunction, mapContext) {\n  if (traverseContextPool.length) {\n    var traverseContext = traverseContextPool.pop();\n    traverseContext.result = mapResult;\n    traverseContext.keyPrefix = keyPrefix;\n    traverseContext.func = mapFunction;\n    traverseContext.context = mapContext;\n    traverseContext.count = 0;\n    return traverseContext;\n  } else {\n    return {\n      result: mapResult,\n      keyPrefix: keyPrefix,\n      func: mapFunction,\n      context: mapContext,\n      count: 0\n    };\n  }\n}\n\nfunction releaseTraverseContext(traverseContext) {\n  traverseContext.result = null;\n  traverseContext.keyPrefix = null;\n  traverseContext.func = null;\n  traverseContext.context = null;\n  traverseContext.count = 0;\n  if (traverseContextPool.length < POOL_SIZE) {\n    traverseContextPool.push(traverseContext);\n  }\n}\n\n/**\n * @param {?*} children Children tree container.\n * @param {!string} nameSoFar Name of the key path so far.\n * @param {!function} callback Callback to invoke with each child found.\n * @param {?*} traverseContext Used to pass information throughout the traversal\n * process.\n * @return {!number} The number of children in this subtree.\n */\nfunction traverseAllChildrenImpl(children, nameSoFar, callback, traverseContext) {\n  var type = typeof children;\n\n  if (type === 'undefined' || type === 'boolean') {\n    // All of the above are perceived as null.\n    children = null;\n  }\n\n  var invokeCallback = false;\n\n  if (children === null) {\n    invokeCallback = true;\n  } else {\n    switch (type) {\n      case 'string':\n      case 'number':\n        invokeCallback = true;\n        break;\n      case 'object':\n        switch (children.$$typeof) {\n          case REACT_ELEMENT_TYPE:\n          case REACT_CALL_TYPE:\n          case REACT_RETURN_TYPE:\n          case REACT_PORTAL_TYPE:\n            invokeCallback = true;\n        }\n    }\n  }\n\n  if (invokeCallback) {\n    callback(traverseContext, children,\n    // If it's the only child, treat the name as if it was wrapped in an array\n    // so that it's consistent if the number of children grows.\n    nameSoFar === '' ? SEPARATOR + getComponentKey(children, 0) : nameSoFar);\n    return 1;\n  }\n\n  var child;\n  var nextName;\n  var subtreeCount = 0; // Count of children found in the current subtree.\n  var nextNamePrefix = nameSoFar === '' ? SEPARATOR : nameSoFar + SUBSEPARATOR;\n\n  if (Array.isArray(children)) {\n    for (var i = 0; i < children.length; i++) {\n      child = children[i];\n      nextName = nextNamePrefix + getComponentKey(child, i);\n      subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);\n    }\n  } else {\n    var iteratorFn = getIteratorFn(children);\n    if (typeof iteratorFn === 'function') {\n      {\n        // Warn about using Maps as children\n        if (iteratorFn === children.entries) {\n          warning(didWarnAboutMaps, 'Using Maps as children is unsupported and will likely yield ' + 'unexpected results. Convert it to a sequence/iterable of keyed ' + 'ReactElements instead.%s', ReactDebugCurrentFrame.getStackAddendum());\n          didWarnAboutMaps = true;\n        }\n      }\n\n      var iterator = iteratorFn.call(children);\n      var step;\n      var ii = 0;\n      while (!(step = iterator.next()).done) {\n        child = step.value;\n        nextName = nextNamePrefix + getComponentKey(child, ii++);\n        subtreeCount += traverseAllChildrenImpl(child, nextName, callback, traverseContext);\n      }\n    } else if (type === 'object') {\n      var addendum = '';\n      {\n        addendum = ' If you meant to render a collection of children, use an array ' + 'instead.' + ReactDebugCurrentFrame.getStackAddendum();\n      }\n      var childrenString = '' + children;\n      invariant(false, 'Objects are not valid as a React child (found: %s).%s', childrenString === '[object Object]' ? 'object with keys {' + Object.keys(children).join(', ') + '}' : childrenString, addendum);\n    }\n  }\n\n  return subtreeCount;\n}\n\n/**\n * Traverses children that are typically specified as `props.children`, but\n * might also be specified through attributes:\n *\n * - `traverseAllChildren(this.props.children, ...)`\n * - `traverseAllChildren(this.props.leftPanelChildren, ...)`\n *\n * The `traverseContext` is an optional argument that is passed through the\n * entire traversal. It can be used to store accumulations or anything else that\n * the callback might find relevant.\n *\n * @param {?*} children Children tree object.\n * @param {!function} callback To invoke upon traversing each child.\n * @param {?*} traverseContext Context for traversal.\n * @return {!number} The number of children in this subtree.\n */\nfunction traverseAllChildren(children, callback, traverseContext) {\n  if (children == null) {\n    return 0;\n  }\n\n  return traverseAllChildrenImpl(children, '', callback, traverseContext);\n}\n\n/**\n * Generate a key string that identifies a component within a set.\n *\n * @param {*} component A component that could contain a manual key.\n * @param {number} index Index that is used if a manual key is not provided.\n * @return {string}\n */\nfunction getComponentKey(component, index) {\n  // Do some typechecking here since we call this blindly. We want to ensure\n  // that we don't block potential future ES APIs.\n  if (typeof component === 'object' && component !== null && component.key != null) {\n    // Explicit key\n    return escape(component.key);\n  }\n  // Implicit key determined by the index in the set\n  return index.toString(36);\n}\n\nfunction forEachSingleChild(bookKeeping, child, name) {\n  var func = bookKeeping.func,\n      context = bookKeeping.context;\n\n  func.call(context, child, bookKeeping.count++);\n}\n\n/**\n * Iterates through children that are typically specified as `props.children`.\n *\n * See https://reactjs.org/docs/react-api.html#react.children.foreach\n *\n * The provided forEachFunc(child, index) will be called for each\n * leaf child.\n *\n * @param {?*} children Children tree container.\n * @param {function(*, int)} forEachFunc\n * @param {*} forEachContext Context for forEachContext.\n */\nfunction forEachChildren(children, forEachFunc, forEachContext) {\n  if (children == null) {\n    return children;\n  }\n  var traverseContext = getPooledTraverseContext(null, null, forEachFunc, forEachContext);\n  traverseAllChildren(children, forEachSingleChild, traverseContext);\n  releaseTraverseContext(traverseContext);\n}\n\nfunction mapSingleChildIntoContext(bookKeeping, child, childKey) {\n  var result = bookKeeping.result,\n      keyPrefix = bookKeeping.keyPrefix,\n      func = bookKeeping.func,\n      context = bookKeeping.context;\n\n\n  var mappedChild = func.call(context, child, bookKeeping.count++);\n  if (Array.isArray(mappedChild)) {\n    mapIntoWithKeyPrefixInternal(mappedChild, result, childKey, emptyFunction.thatReturnsArgument);\n  } else if (mappedChild != null) {\n    if (isValidElement(mappedChild)) {\n      mappedChild = cloneAndReplaceKey(mappedChild,\n      // Keep both the (mapped) and old keys if they differ, just as\n      // traverseAllChildren used to do for objects as children\n      keyPrefix + (mappedChild.key && (!child || child.key !== mappedChild.key) ? escapeUserProvidedKey(mappedChild.key) + '/' : '') + childKey);\n    }\n    result.push(mappedChild);\n  }\n}\n\nfunction mapIntoWithKeyPrefixInternal(children, array, prefix, func, context) {\n  var escapedPrefix = '';\n  if (prefix != null) {\n    escapedPrefix = escapeUserProvidedKey(prefix) + '/';\n  }\n  var traverseContext = getPooledTraverseContext(array, escapedPrefix, func, context);\n  traverseAllChildren(children, mapSingleChildIntoContext, traverseContext);\n  releaseTraverseContext(traverseContext);\n}\n\n/**\n * Maps children that are typically specified as `props.children`.\n *\n * See https://reactjs.org/docs/react-api.html#react.children.map\n *\n * The provided mapFunction(child, key, index) will be called for each\n * leaf child.\n *\n * @param {?*} children Children tree container.\n * @param {function(*, int)} func The map function.\n * @param {*} context Context for mapFunction.\n * @return {object} Object containing the ordered map of results.\n */\nfunction mapChildren(children, func, context) {\n  if (children == null) {\n    return children;\n  }\n  var result = [];\n  mapIntoWithKeyPrefixInternal(children, result, null, func, context);\n  return result;\n}\n\n/**\n * Count the number of children that are typically specified as\n * `props.children`.\n *\n * See https://reactjs.org/docs/react-api.html#react.children.count\n *\n * @param {?*} children Children tree container.\n * @return {number} The number of children.\n */\nfunction countChildren(children, context) {\n  return traverseAllChildren(children, emptyFunction.thatReturnsNull, null);\n}\n\n/**\n * Flatten a children object (typically specified as `props.children`) and\n * return an array with appropriately re-keyed children.\n *\n * See https://reactjs.org/docs/react-api.html#react.children.toarray\n */\nfunction toArray(children) {\n  var result = [];\n  mapIntoWithKeyPrefixInternal(children, result, null, emptyFunction.thatReturnsArgument);\n  return result;\n}\n\n/**\n * Returns the first child in a collection of children and verifies that there\n * is only one child in the collection.\n *\n * See https://reactjs.org/docs/react-api.html#react.children.only\n *\n * The current implementation of this function assumes that a single child gets\n * passed without a wrapper, but the purpose of this helper function is to\n * abstract away the particular structure of children.\n *\n * @param {?object} children Child collection structure.\n * @return {ReactElement} The first and only `ReactElement` contained in the\n * structure.\n */\nfunction onlyChild(children) {\n  !isValidElement(children) ? invariant(false, 'React.Children.only expected to receive a single React element child.') : void 0;\n  return children;\n}\n\nvar describeComponentFrame = function (name, source, ownerName) {\n  return '\\n    in ' + (name || 'Unknown') + (source ? ' (at ' + source.fileName.replace(/^.*[\\\\\\/]/, '') + ':' + source.lineNumber + ')' : ownerName ? ' (created by ' + ownerName + ')' : '');\n};\n\nfunction getComponentName(fiber) {\n  var type = fiber.type;\n\n  if (typeof type === 'string') {\n    return type;\n  }\n  if (typeof type === 'function') {\n    return type.displayName || type.name;\n  }\n  return null;\n}\n\n/**\n * ReactElementValidator provides a wrapper around a element factory\n * which validates the props passed to the element. This is intended to be\n * used only in DEV and could be replaced by a static type checker for languages\n * that support it.\n */\n\n{\n  var currentlyValidatingElement = null;\n\n  var propTypesMisspellWarningShown = false;\n\n  var getDisplayName = function (element) {\n    if (element == null) {\n      return '#empty';\n    } else if (typeof element === 'string' || typeof element === 'number') {\n      return '#text';\n    } else if (typeof element.type === 'string') {\n      return element.type;\n    } else if (element.type === REACT_FRAGMENT_TYPE) {\n      return 'React.Fragment';\n    } else {\n      return element.type.displayName || element.type.name || 'Unknown';\n    }\n  };\n\n  var getStackAddendum = function () {\n    var stack = '';\n    if (currentlyValidatingElement) {\n      var name = getDisplayName(currentlyValidatingElement);\n      var owner = currentlyValidatingElement._owner;\n      stack += describeComponentFrame(name, currentlyValidatingElement._source, owner && getComponentName(owner));\n    }\n    stack += ReactDebugCurrentFrame.getStackAddendum() || '';\n    return stack;\n  };\n\n  var VALID_FRAGMENT_PROPS = new Map([['children', true], ['key', true]]);\n}\n\nfunction getDeclarationErrorAddendum() {\n  if (ReactCurrentOwner.current) {\n    var name = getComponentName(ReactCurrentOwner.current);\n    if (name) {\n      return '\\n\\nCheck the render method of `' + name + '`.';\n    }\n  }\n  return '';\n}\n\nfunction getSourceInfoErrorAddendum(elementProps) {\n  if (elementProps !== null && elementProps !== undefined && elementProps.__source !== undefined) {\n    var source = elementProps.__source;\n    var fileName = source.fileName.replace(/^.*[\\\\\\/]/, '');\n    var lineNumber = source.lineNumber;\n    return '\\n\\nCheck your code at ' + fileName + ':' + lineNumber + '.';\n  }\n  return '';\n}\n\n/**\n * Warn if there's no key explicitly set on dynamic arrays of children or\n * object keys are not valid. This allows us to keep track of children between\n * updates.\n */\nvar ownerHasKeyUseWarning = {};\n\nfunction getCurrentComponentErrorInfo(parentType) {\n  var info = getDeclarationErrorAddendum();\n\n  if (!info) {\n    var parentName = typeof parentType === 'string' ? parentType : parentType.displayName || parentType.name;\n    if (parentName) {\n      info = '\\n\\nCheck the top-level render call using <' + parentName + '>.';\n    }\n  }\n  return info;\n}\n\n/**\n * Warn if the element doesn't have an explicit key assigned to it.\n * This element is in an array. The array could grow and shrink or be\n * reordered. All children that haven't already been validated are required to\n * have a \"key\" property assigned to it. Error statuses are cached so a warning\n * will only be shown once.\n *\n * @internal\n * @param {ReactElement} element Element that requires a key.\n * @param {*} parentType element's parent's type.\n */\nfunction validateExplicitKey(element, parentType) {\n  if (!element._store || element._store.validated || element.key != null) {\n    return;\n  }\n  element._store.validated = true;\n\n  var currentComponentErrorInfo = getCurrentComponentErrorInfo(parentType);\n  if (ownerHasKeyUseWarning[currentComponentErrorInfo]) {\n    return;\n  }\n  ownerHasKeyUseWarning[currentComponentErrorInfo] = true;\n\n  // Usually the current owner is the offender, but if it accepts children as a\n  // property, it may be the creator of the child that's responsible for\n  // assigning it a key.\n  var childOwner = '';\n  if (element && element._owner && element._owner !== ReactCurrentOwner.current) {\n    // Give the component that originally created this child.\n    childOwner = ' It was passed a child from ' + getComponentName(element._owner) + '.';\n  }\n\n  currentlyValidatingElement = element;\n  {\n    warning(false, 'Each child in an array or iterator should have a unique \"key\" prop.' + '%s%s See https://fb.me/react-warning-keys for more information.%s', currentComponentErrorInfo, childOwner, getStackAddendum());\n  }\n  currentlyValidatingElement = null;\n}\n\n/**\n * Ensure that every element either is passed in a static location, in an\n * array with an explicit keys property defined, or in an object literal\n * with valid key property.\n *\n * @internal\n * @param {ReactNode} node Statically passed child of any type.\n * @param {*} parentType node's parent's type.\n */\nfunction validateChildKeys(node, parentType) {\n  if (typeof node !== 'object') {\n    return;\n  }\n  if (Array.isArray(node)) {\n    for (var i = 0; i < node.length; i++) {\n      var child = node[i];\n      if (isValidElement(child)) {\n        validateExplicitKey(child, parentType);\n      }\n    }\n  } else if (isValidElement(node)) {\n    // This element was passed in a valid location.\n    if (node._store) {\n      node._store.validated = true;\n    }\n  } else if (node) {\n    var iteratorFn = getIteratorFn(node);\n    if (typeof iteratorFn === 'function') {\n      // Entry iterators used to provide implicit keys,\n      // but now we print a separate warning for them later.\n      if (iteratorFn !== node.entries) {\n        var iterator = iteratorFn.call(node);\n        var step;\n        while (!(step = iterator.next()).done) {\n          if (isValidElement(step.value)) {\n            validateExplicitKey(step.value, parentType);\n          }\n        }\n      }\n    }\n  }\n}\n\n/**\n * Given an element, validate that its props follow the propTypes definition,\n * provided by the type.\n *\n * @param {ReactElement} element\n */\nfunction validatePropTypes(element) {\n  var componentClass = element.type;\n  if (typeof componentClass !== 'function') {\n    return;\n  }\n  var name = componentClass.displayName || componentClass.name;\n  var propTypes = componentClass.propTypes;\n  if (propTypes) {\n    currentlyValidatingElement = element;\n    checkPropTypes(propTypes, element.props, 'prop', name, getStackAddendum);\n    currentlyValidatingElement = null;\n  } else if (componentClass.PropTypes !== undefined && !propTypesMisspellWarningShown) {\n    propTypesMisspellWarningShown = true;\n    warning(false, 'Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?', name || 'Unknown');\n  }\n  if (typeof componentClass.getDefaultProps === 'function') {\n    warning(componentClass.getDefaultProps.isReactClassApproved, 'getDefaultProps is only used on classic React.createClass ' + 'definitions. Use a static property named `defaultProps` instead.');\n  }\n}\n\n/**\n * Given a fragment, validate that it can only be provided with fragment props\n * @param {ReactElement} fragment\n */\nfunction validateFragmentProps(fragment) {\n  currentlyValidatingElement = fragment;\n\n  var _iteratorNormalCompletion = true;\n  var _didIteratorError = false;\n  var _iteratorError = undefined;\n\n  try {\n    for (var _iterator = Object.keys(fragment.props)[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {\n      var key = _step.value;\n\n      if (!VALID_FRAGMENT_PROPS.has(key)) {\n        warning(false, 'Invalid prop `%s` supplied to `React.Fragment`. ' + 'React.Fragment can only have `key` and `children` props.%s', key, getStackAddendum());\n        break;\n      }\n    }\n  } catch (err) {\n    _didIteratorError = true;\n    _iteratorError = err;\n  } finally {\n    try {\n      if (!_iteratorNormalCompletion && _iterator['return']) {\n        _iterator['return']();\n      }\n    } finally {\n      if (_didIteratorError) {\n        throw _iteratorError;\n      }\n    }\n  }\n\n  if (fragment.ref !== null) {\n    warning(false, 'Invalid attribute `ref` supplied to `React.Fragment`.%s', getStackAddendum());\n  }\n\n  currentlyValidatingElement = null;\n}\n\nfunction createElementWithValidation(type, props, children) {\n  var validType = typeof type === 'string' || typeof type === 'function' || typeof type === 'symbol' || typeof type === 'number';\n  // We warn in this case but don't throw. We expect the element creation to\n  // succeed and there will likely be errors in render.\n  if (!validType) {\n    var info = '';\n    if (type === undefined || typeof type === 'object' && type !== null && Object.keys(type).length === 0) {\n      info += ' You likely forgot to export your component from the file ' + \"it's defined in, or you might have mixed up default and named imports.\";\n    }\n\n    var sourceInfo = getSourceInfoErrorAddendum(props);\n    if (sourceInfo) {\n      info += sourceInfo;\n    } else {\n      info += getDeclarationErrorAddendum();\n    }\n\n    info += getStackAddendum() || '';\n\n    warning(false, 'React.createElement: type is invalid -- expected a string (for ' + 'built-in components) or a class/function (for composite ' + 'components) but got: %s.%s', type == null ? type : typeof type, info);\n  }\n\n  var element = createElement.apply(this, arguments);\n\n  // The result can be nullish if a mock or a custom function is used.\n  // TODO: Drop this when these are no longer allowed as the type argument.\n  if (element == null) {\n    return element;\n  }\n\n  // Skip key warning if the type isn't valid since our key validation logic\n  // doesn't expect a non-string/function type and can throw confusing errors.\n  // We don't want exception behavior to differ between dev and prod.\n  // (Rendering will throw with a helpful message and as soon as the type is\n  // fixed, the key warnings will appear.)\n  if (validType) {\n    for (var i = 2; i < arguments.length; i++) {\n      validateChildKeys(arguments[i], type);\n    }\n  }\n\n  if (typeof type === 'symbol' && type === REACT_FRAGMENT_TYPE) {\n    validateFragmentProps(element);\n  } else {\n    validatePropTypes(element);\n  }\n\n  return element;\n}\n\nfunction createFactoryWithValidation(type) {\n  var validatedFactory = createElementWithValidation.bind(null, type);\n  // Legacy hook TODO: Warn if this is accessed\n  validatedFactory.type = type;\n\n  {\n    Object.defineProperty(validatedFactory, 'type', {\n      enumerable: false,\n      get: function () {\n        lowPriorityWarning$1(false, 'Factory.type is deprecated. Access the class directly ' + 'before passing it to createFactory.');\n        Object.defineProperty(this, 'type', {\n          value: type\n        });\n        return type;\n      }\n    });\n  }\n\n  return validatedFactory;\n}\n\nfunction cloneElementWithValidation(element, props, children) {\n  var newElement = cloneElement.apply(this, arguments);\n  for (var i = 2; i < arguments.length; i++) {\n    validateChildKeys(arguments[i], newElement.type);\n  }\n  validatePropTypes(newElement);\n  return newElement;\n}\n\nvar React = {\n  Children: {\n    map: mapChildren,\n    forEach: forEachChildren,\n    count: countChildren,\n    toArray: toArray,\n    only: onlyChild\n  },\n\n  Component: Component,\n  PureComponent: PureComponent,\n  unstable_AsyncComponent: AsyncComponent,\n\n  Fragment: REACT_FRAGMENT_TYPE,\n\n  createElement: createElementWithValidation,\n  cloneElement: cloneElementWithValidation,\n  createFactory: createFactoryWithValidation,\n  isValidElement: isValidElement,\n\n  version: ReactVersion,\n\n  __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED: {\n    ReactCurrentOwner: ReactCurrentOwner,\n    // Used by renderers to avoid bundling object-assign twice in UMD bundles:\n    assign: _assign\n  }\n};\n\n{\n  _assign(React.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED, {\n    // These should not be included in production.\n    ReactDebugCurrentFrame: ReactDebugCurrentFrame,\n    // Shim for React DOM 16.0.0 which still destructured (but not used) this.\n    // TODO: remove in React 17.0.\n    ReactComponentTreeHook: {}\n  });\n}\n\n\n\nvar React$2 = Object.freeze({\n\tdefault: React\n});\n\nvar React$3 = ( React$2 && React ) || React$2;\n\n// TODO: decide on the top-level export form.\n// This is hacky but makes it work with both Rollup and Jest.\nvar react = React$3['default'] ? React$3['default'] : React$3;\n\nmodule.exports = react;\n  })();\n}\n\n\n//# sourceURL=webpack:///./node_modules/react/cjs/react.development.js?")},"./node_modules/react/index.js":function(module,exports,__webpack_require__){"use strict";eval('\n\nif (false) {} else {\n  module.exports = __webpack_require__(/*! ./cjs/react.development.js */ "./node_modules/react/cjs/react.development.js");\n}\n\n\n//# sourceURL=webpack:///./node_modules/react/index.js?')},"./node_modules/setimmediate/setImmediate.js":function(module,exports,__webpack_require__){eval('/* WEBPACK VAR INJECTION */(function(global, process) {(function (global, undefined) {\n    "use strict";\n\n    if (global.setImmediate) {\n        return;\n    }\n\n    var nextHandle = 1; // Spec says greater than zero\n    var tasksByHandle = {};\n    var currentlyRunningATask = false;\n    var doc = global.document;\n    var registerImmediate;\n\n    function setImmediate(callback) {\n      // Callback can either be a function or a string\n      if (typeof callback !== "function") {\n        callback = new Function("" + callback);\n      }\n      // Copy function arguments\n      var args = new Array(arguments.length - 1);\n      for (var i = 0; i < args.length; i++) {\n          args[i] = arguments[i + 1];\n      }\n      // Store and register the task\n      var task = { callback: callback, args: args };\n      tasksByHandle[nextHandle] = task;\n      registerImmediate(nextHandle);\n      return nextHandle++;\n    }\n\n    function clearImmediate(handle) {\n        delete tasksByHandle[handle];\n    }\n\n    function run(task) {\n        var callback = task.callback;\n        var args = task.args;\n        switch (args.length) {\n        case 0:\n            callback();\n            break;\n        case 1:\n            callback(args[0]);\n            break;\n        case 2:\n            callback(args[0], args[1]);\n            break;\n        case 3:\n            callback(args[0], args[1], args[2]);\n            break;\n        default:\n            callback.apply(undefined, args);\n            break;\n        }\n    }\n\n    function runIfPresent(handle) {\n        // From the spec: "Wait until any invocations of this algorithm started before this one have completed."\n        // So if we\'re currently running a task, we\'ll need to delay this invocation.\n        if (currentlyRunningATask) {\n            // Delay by doing a setTimeout. setImmediate was tried instead, but in Firefox 7 it generated a\n            // "too much recursion" error.\n            setTimeout(runIfPresent, 0, handle);\n        } else {\n            var task = tasksByHandle[handle];\n            if (task) {\n                currentlyRunningATask = true;\n                try {\n                    run(task);\n                } finally {\n                    clearImmediate(handle);\n                    currentlyRunningATask = false;\n                }\n            }\n        }\n    }\n\n    function installNextTickImplementation() {\n        registerImmediate = function(handle) {\n            process.nextTick(function () { runIfPresent(handle); });\n        };\n    }\n\n    function canUsePostMessage() {\n        // The test against `importScripts` prevents this implementation from being installed inside a web worker,\n        // where `global.postMessage` means something completely different and can\'t be used for this purpose.\n        if (global.postMessage && !global.importScripts) {\n            var postMessageIsAsynchronous = true;\n            var oldOnMessage = global.onmessage;\n            global.onmessage = function() {\n                postMessageIsAsynchronous = false;\n            };\n            global.postMessage("", "*");\n            global.onmessage = oldOnMessage;\n            return postMessageIsAsynchronous;\n        }\n    }\n\n    function installPostMessageImplementation() {\n        // Installs an event handler on `global` for the `message` event: see\n        // * https://developer.mozilla.org/en/DOM/window.postMessage\n        // * http://www.whatwg.org/specs/web-apps/current-work/multipage/comms.html#crossDocumentMessages\n\n        var messagePrefix = "setImmediate$" + Math.random() + "$";\n        var onGlobalMessage = function(event) {\n            if (event.source === global &&\n                typeof event.data === "string" &&\n                event.data.indexOf(messagePrefix) === 0) {\n                runIfPresent(+event.data.slice(messagePrefix.length));\n            }\n        };\n\n        if (global.addEventListener) {\n            global.addEventListener("message", onGlobalMessage, false);\n        } else {\n            global.attachEvent("onmessage", onGlobalMessage);\n        }\n\n        registerImmediate = function(handle) {\n            global.postMessage(messagePrefix + handle, "*");\n        };\n    }\n\n    function installMessageChannelImplementation() {\n        var channel = new MessageChannel();\n        channel.port1.onmessage = function(event) {\n            var handle = event.data;\n            runIfPresent(handle);\n        };\n\n        registerImmediate = function(handle) {\n            channel.port2.postMessage(handle);\n        };\n    }\n\n    function installReadyStateChangeImplementation() {\n        var html = doc.documentElement;\n        registerImmediate = function(handle) {\n            // Create a <script> element; its readystatechange event will be fired asynchronously once it is inserted\n            // into the document. Do so, thus queuing up the task. Remember to clean up once it\'s been called.\n            var script = doc.createElement("script");\n            script.onreadystatechange = function () {\n                runIfPresent(handle);\n                script.onreadystatechange = null;\n                html.removeChild(script);\n                script = null;\n            };\n            html.appendChild(script);\n        };\n    }\n\n    function installSetTimeoutImplementation() {\n        registerImmediate = function(handle) {\n            setTimeout(runIfPresent, 0, handle);\n        };\n    }\n\n    // If supported, we should attach to the prototype of global, since that is where setTimeout et al. live.\n    var attachTo = Object.getPrototypeOf && Object.getPrototypeOf(global);\n    attachTo = attachTo && attachTo.setTimeout ? attachTo : global;\n\n    // Don\'t get fooled by e.g. browserify environments.\n    if ({}.toString.call(global.process) === "[object process]") {\n        // For Node.js before 0.9\n        installNextTickImplementation();\n\n    } else if (canUsePostMessage()) {\n        // For non-IE10 modern browsers\n        installPostMessageImplementation();\n\n    } else if (global.MessageChannel) {\n        // For web workers, where supported\n        installMessageChannelImplementation();\n\n    } else if (doc && "onreadystatechange" in doc.createElement("script")) {\n        // For IE 6–8\n        installReadyStateChangeImplementation();\n\n    } else {\n        // For older browsers\n        installSetTimeoutImplementation();\n    }\n\n    attachTo.setImmediate = setImmediate;\n    attachTo.clearImmediate = clearImmediate;\n}(typeof self === "undefined" ? typeof global === "undefined" ? this : global : self));\n\n/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js"), __webpack_require__(/*! ./../process/browser.js */ "./node_modules/process/browser.js")))\n\n//# sourceURL=webpack:///./node_modules/setimmediate/setImmediate.js?')},"./node_modules/ticky/ticky-browser.js":function(module,exports,__webpack_require__){eval("/* WEBPACK VAR INJECTION */(function(setImmediate) {var si = typeof setImmediate === 'function', tick;\nif (si) {\n  tick = function (fn) { setImmediate(fn); };\n} else {\n  tick = function (fn) { setTimeout(fn, 0); };\n}\n\nmodule.exports = tick;\n/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../timers-browserify/main.js */ \"./node_modules/timers-browserify/main.js\").setImmediate))\n\n//# sourceURL=webpack:///./node_modules/ticky/ticky-browser.js?")},"./node_modules/timers-browserify/main.js":function(module,exports,__webpack_require__){eval('/* WEBPACK VAR INJECTION */(function(global) {var apply = Function.prototype.apply;\n\n// DOM APIs, for completeness\n\nexports.setTimeout = function() {\n  return new Timeout(apply.call(setTimeout, window, arguments), clearTimeout);\n};\nexports.setInterval = function() {\n  return new Timeout(apply.call(setInterval, window, arguments), clearInterval);\n};\nexports.clearTimeout =\nexports.clearInterval = function(timeout) {\n  if (timeout) {\n    timeout.close();\n  }\n};\n\nfunction Timeout(id, clearFn) {\n  this._id = id;\n  this._clearFn = clearFn;\n}\nTimeout.prototype.unref = Timeout.prototype.ref = function() {};\nTimeout.prototype.close = function() {\n  this._clearFn.call(window, this._id);\n};\n\n// Does not start the time, just sets up the members needed.\nexports.enroll = function(item, msecs) {\n  clearTimeout(item._idleTimeoutId);\n  item._idleTimeout = msecs;\n};\n\nexports.unenroll = function(item) {\n  clearTimeout(item._idleTimeoutId);\n  item._idleTimeout = -1;\n};\n\nexports._unrefActive = exports.active = function(item) {\n  clearTimeout(item._idleTimeoutId);\n\n  var msecs = item._idleTimeout;\n  if (msecs >= 0) {\n    item._idleTimeoutId = setTimeout(function onTimeout() {\n      if (item._onTimeout)\n        item._onTimeout();\n    }, msecs);\n  }\n};\n\n// setimmediate attaches itself to the global object\n__webpack_require__(/*! setimmediate */ "./node_modules/setimmediate/setImmediate.js");\n// On some exotic environments, it\'s not clear which object `setimmeidate` was\n// able to install onto.  Search each possibility in the same order as the\n// `setimmediate` library.\nexports.setImmediate = (typeof self !== "undefined" && self.setImmediate) ||\n                       (typeof global !== "undefined" && global.setImmediate) ||\n                       (this && this.setImmediate);\nexports.clearImmediate = (typeof self !== "undefined" && self.clearImmediate) ||\n                         (typeof global !== "undefined" && global.clearImmediate) ||\n                         (this && this.clearImmediate);\n\n/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js")))\n\n//# sourceURL=webpack:///./node_modules/timers-browserify/main.js?')},"./node_modules/webpack/buildin/global.js":function(module,exports){eval('var g;\r\n\r\n// This works in non-strict mode\r\ng = (function() {\r\n\treturn this;\r\n})();\r\n\r\ntry {\r\n\t// This works if eval is allowed (see CSP)\r\n\tg = g || Function("return this")() || (1, eval)("this");\r\n} catch (e) {\r\n\t// This works if the window reference is available\r\n\tif (typeof window === "object") g = window;\r\n}\r\n\r\n// g can still be undefined, but nothing to do about it...\r\n// We return undefined, instead of nothing here, so it\'s\r\n// easier to handle this case. if(!global) { ...}\r\n\r\nmodule.exports = g;\r\n\n\n//# sourceURL=webpack:///(webpack)/buildin/global.js?')}});